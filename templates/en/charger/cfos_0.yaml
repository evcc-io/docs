product:
  brand: cFos
  description: PowerBrain
capabilities: ["mA", "rfid"]
requirements: ["sponsorship"]
description: |
  The meter- if present- must be configured separately as charge meter.
render:
  - default: |
      type: template
      template: cfos
      host: 192.0.2.2 # IP address or hostname
