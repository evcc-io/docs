product:
  brand: Zuidwijk
  description: SlimmeLezer(+) V2
description: |
  More recent slimmelezer devices use a different configuration. Try this template if the other one fails.
render:
  - usage: grid
    default: |
      type: template
      template: slimmelezer-V2
      usage: grid
      host: 192.0.2.2
