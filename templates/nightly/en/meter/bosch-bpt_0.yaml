product:
  brand: Bosch
  description: BPT-S 5 Hybrid
requirements: ["skiptest"]
render:
  - usage: grid
    default: |
      type: template
      template: bosch-bpt
      usage: grid
      uri: # HTTP(S) address (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: grid
      uri: # HTTP(S) address (optional)
  - usage: pv
    default: |
      type: template
      template: bosch-bpt
      usage: pv
      uri: # HTTP(S) address (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: pv
      uri: # HTTP(S) address (optional)
  - usage: battery
    default: |
      type: template
      template: bosch-bpt
      usage: battery
      uri: # HTTP(S) address (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: battery
      uri: # HTTP(S) address (optional)
      capacity: 50 # optional
