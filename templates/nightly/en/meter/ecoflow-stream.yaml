template: ecoflow-stream
product:
  identifier: ecoflow-stream
  brand: EcoFlow
  description: Stream
description: |
  To use the EcoFlow Stream meter you need:
  - A valid Access Key from the EcoFlow Developer Console
  - The corresponding Secret Key
  - The main device serial number of your Stream system

  These credentials can be obtained through the EcoFlow Developer Console after registering your application.

render:
  - usage: grid
    default: |
      type: template
      template: ecoflow-stream
      usage: grid
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
    advanced: |
      type: template
      template: ecoflow-stream
      usage: grid
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
      cache: 30s # Cache, Time interval for data refresh (optional)
  - usage: pv
    default: |
      type: template
      template: ecoflow-stream
      usage: pv
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
    advanced: |
      type: template
      template: ecoflow-stream
      usage: pv
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
      cache: 30s # Cache, Time interval for data refresh (optional)
  - usage: battery
    default: |
      type: template
      template: ecoflow-stream
      usage: battery
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
    advanced: |
      type: template
      template: ecoflow-stream
      usage: battery
      accesskey: # Access Key, Access Key from EcoFlow Developer Console
      secretkey: # Secret Key, Secret Key from EcoFlow Developer Console
      serial: # Serial, optional
      region: auto # API Region: auto (default), europe, or america., If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america. [auto, europe, america] (optional)
      cache: 30s # Cache, Time interval for data refresh (optional)
params:
  - name: accesskey
    example:
    default:
    choice: []
    unit:
    description: Access Key
    help: Access Key from EcoFlow Developer Console
    advanced: false
    optional: false
  - name: secretkey
    example:
    default:
    choice: []
    unit:
    description: Secret Key
    help: Secret Key from EcoFlow Developer Console
    advanced: false
    optional: false
  - name: serial
    example:
    default:
    choice: []
    unit:
    description: Serial
    help:
    advanced: false
    optional: true
  - name: region
    example:
    default: auto
    choice: ['auto', 'europe', 'america']
    unit:
    description: 'API Region: auto (default), europe, or america.'
    help: 'If you sometimes see an error "8513: accessKey is invalid", which for example Starlink users do, you can set API region explicitly. Passible values: auto (default), europe, or america.'
    advanced: false
    optional: true
  - name: cache
    example: 5m
    default: 30s
    choice: []
    unit:
    description: Cache
    help: Time interval for data refresh
    advanced: true
    optional: true