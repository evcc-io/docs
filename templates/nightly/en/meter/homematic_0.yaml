product:
  brand: Homematic IP
  group: Switchable sockets
render:
  - usage: grid
    default: |
      type: template
      template: homematic
      usage: grid
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
    advanced: |
      type: template
      template: homematic
      usage: grid
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
      cache: 1s # In case of duty cycle problems try a cache setting of 30s. (optional)
  - usage: pv
    default: |
      type: template
      template: homematic
      usage: pv
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
    advanced: |
      type: template
      template: homematic
      usage: pv
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
      cache: 1s # In case of duty cycle problems try a cache setting of 30s. (optional)
  - usage: charge
    default: |
      type: template
      template: homematic
      usage: charge
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
    advanced: |
      type: template
      template: homematic
      usage: charge
      host: 192.0.2.2
      device: 0001EE89AAD848 # Homematic device id like shown in the CCU web user interface.
      user: # User account (e.g. email address, user id, etc.) (optional)
      password: # Password of the user account (use single quotes in case of leading zeros) (optional)
      meterchannel: 6 # Homematic meter channel number like shown after the device id separated with a colon in the CCU web user interface.
      cache: 1s # In case of duty cycle problems try a cache setting of 30s. (optional)
