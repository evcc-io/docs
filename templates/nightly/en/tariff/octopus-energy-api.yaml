template: octopus-api
product:
  identifier: octopus-energy-api
  brand: Octopus Energy
  description: API
  group: Dynamic electricity price
countries: ["GB"]
description: |
  You can get the API key in the Octopus portal https://octopus.energy/dashboard/new/accounts/personal-details/api-access
render:
  - default: |
      type: template
      template: octopus-api
      apiKey: # Octopus Energy API Key.
      accountNumber: # Optional Account Number (usually in the format X-XXXXXXXX). Only required if you have multiple Accounts. (optional)
params:
  - name: apiKey
    example:
    default:
    choice: []
    unit:
    description:
    help: Octopus Energy API Key.
    advanced: false
    optional: false
  - name: accountNumber
    example:
    default:
    choice: []
    unit:
    description:
    help: Optional Account Number (usually in the format X-XXXXXXXX). Only required if you have multiple Accounts.
    advanced: false
    optional: true