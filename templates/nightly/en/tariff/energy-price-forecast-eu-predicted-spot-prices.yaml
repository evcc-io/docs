template: energypriceforecast
product:
  identifier: energy-price-forecast-eu-predicted-spot-prices
  brand: Energy Price Forecast EU
  description: Predicted spot prices
  group: Dynamic electricity price
countries: ["DE", "NL", "BE", "DK", "FR", "AT", "CZ", "PL", "FI", "SE", "NO"]
requirements: ["skiptest"]
description: |
  Spot prices in day-ahead market plus 4 days prognosis [energypriceforecast.eu](https://energypriceforecast.eu)
render:
  - default: |
      type: template
      template: energypriceforecast
      region: DE # Region, [DE, NL, BE, DK1, DK2, FR, AT, CZ, PL, FI, SE1, SE2, SE3, SE4, NO1, NO2, NO3, NO4, NO5]
      currency: EUR # Currency, [EUR, DKK, CZK, PLN, SEK, NOK]
      horizon: 120 # Forecast horizon (h)
    advanced: |
      type: template
      template: energypriceforecast
      region: DE # Region, [DE, NL, BE, DK1, DK2, FR, AT, CZ, PL, FI, SE1, SE2, SE3, SE4, NO1, NO2, NO3, NO4, NO5]
      currency: EUR # Currency, [EUR, DKK, CZK, PLN, SEK, NOK]
      horizon: 120 # Forecast horizon (h)
      charges: # Charge, Additional fixed charge per kWh (optional)
      chargesZones: # Charges zones, Time-based grid fees that override the default charge. (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
      average: # Average by hour, optional
params:
  - name: region
    example: DE
    default:
    choice: ['DE', 'NL', 'BE', 'DK1', 'DK2', 'FR', 'AT', 'CZ', 'PL', 'FI', 'SE1', 'SE2', 'SE3', 'SE4', 'NO1', 'NO2', 'NO3', 'NO4', 'NO5']
    unit:
    description: Region
    help:
    advanced: false
    optional: false
  - name: currency
    example:
    default: EUR
    choice: ['EUR', 'DKK', 'CZK', 'PLN', 'SEK', 'NOK']
    unit:
    description: Currency
    help:
    advanced: false
    optional: false
  - name: horizon
    example:
    default: 120
    choice: []
    unit: h
    description: Forecast horizon
    help:
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh
    advanced: true
    optional: true
  - name: chargesZones
    example:
    default:
    choice: []
    unit:
    description: Charges zones
    help: Time-based grid fees that override the default charge.
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true
  - name: average
    example:
    default:
    choice: []
    unit:
    description: Average by hour
    help:
    advanced: true
    optional: true