template: epex-predictor
product:
  identifier: epex-predictor-predicted-epex-spot-prices
  brand: EPEX Predictor
  description: Predicted EPEX spot prices
  group: Dynamic electricity price
countries: ["DE", "AT", "NL", "BE", "SE", "DK"]
requirements: ["skiptest"]
description: |
  EPEX spot price predictions from epexpredictor.batzill.com
render:
  - default: |
      type: template
      template: epex-predictor
      region: DE # Region, [DE, AT, NL, BE, SE_1, SE_2, SE_3, SE_4, DK_1, DK_2]
    advanced: |
      type: template
      template: epex-predictor
      region: DE # Region, [DE, AT, NL, BE, SE_1, SE_2, SE_3, SE_4, DK_1, DK_2]
      uri: https://epexpredictor.batzill.com # API endpoint (for self-hosted instances), HTTP(S) address (optional)
      charges: # Charge, Additional fixed charge per kWh (e.g. 0.05 for 5 cents) (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
params:
  - name: region
    example: DE
    default:
    choice: ['DE', 'AT', 'NL', 'BE', 'SE_1', 'SE_2', 'SE_3', 'SE_4', 'DK_1', 'DK_2']
    unit:
    description: Region
    help:
    advanced: false
    optional: false
  - name: uri
    example:
    default: https://epexpredictor.batzill.com
    choice: []
    unit:
    description: API endpoint (for self-hosted instances)
    help: HTTP(S) address
    advanced: true
    optional: true
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh (e.g. 0.05 for 5 cents)
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true