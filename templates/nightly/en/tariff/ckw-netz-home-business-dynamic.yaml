template: ckw
product:
  identifier: ckw-netz-home-business-dynamic
  brand: CKW
  description: Netz Home/Business dynamic
  group: Dynamic electricity price
countries: ["CH"]
requirements: ["skiptest"]
render:
  - default: |
      type: template
      template: ckw
      tariff: # Tariff name, home_dynamic (below 50 MWh/year) or business_dynamic (above 50 MWh/year) [home_dynamic, business_dynamic]
    advanced: |
      type: template
      template: ckw
      tariff: # Tariff name, home_dynamic (below 50 MWh/year) or business_dynamic (above 50 MWh/year) [home_dynamic, business_dynamic]
      charges: # Charge, Additional fixed charge per kWh (e.g. 0.05 for 5 cents) (optional)
      tax: 0.081 # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
params:
  - name: tariff
    example:
    default:
    choice: ['home_dynamic', 'business_dynamic']
    unit:
    description: Tariff name
    help: home_dynamic (below 50 MWh/year) or business_dynamic (above 50 MWh/year)
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh (e.g. 0.05 for 5 cents)
    advanced: true
    optional: true
  - name: tax
    example:
    default: 0.081
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true