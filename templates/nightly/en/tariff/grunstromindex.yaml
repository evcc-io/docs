template: grünstromindex
product:
  identifier: grunstromindex
  brand: Grünstromindex
  group: CO₂ forecast
countries: ["DE"]
requirements: ["skiptest"]
description: |
  Regional emission data from https://gruenstromindex.de
render:
  - default: |
      type: template
      template: grünstromindex
      zip:
      token: # Token for accessing the API from https://console.corrently.io (optional)
params:
  - name: zip
    example:
    default:
    choice: []
    unit:
    description:
    help:
    advanced: false
    optional: false
  - name: token
    example:
    default:
    choice: []
    unit:
    description:
    help: Token for accessing the API from https://console.corrently.io
    advanced: false
    optional: true