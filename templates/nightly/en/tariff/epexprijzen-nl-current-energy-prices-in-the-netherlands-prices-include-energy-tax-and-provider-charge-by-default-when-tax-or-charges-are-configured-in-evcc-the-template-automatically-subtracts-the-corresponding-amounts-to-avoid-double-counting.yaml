template: epexprijzen-nl
product:
  identifier: epexprijzen-nl-current-energy-prices-in-the-netherlands-prices-include-energy-tax-and-provider-charge-by-default-when-tax-or-charges-are-configured-in-evcc-the-template-automatically-subtracts-the-corresponding-amounts-to-avoid-double-counting
  brand: epexprijzen.nl
  description: Current energy prices in the Netherlands. Prices include energy tax and provider charge by default. When 'tax' or 'charges' are configured in EVCC, the template automatically subtracts the corresponding amounts to avoid double-counting.
  group: Dynamic electricity price
countries: ["NL"]
requirements: ["skiptest"]
render:
  - default: |
      type: template
      template: epexprijzen-nl
      provider: # Energy provider, [anwb-energie, atoom-alliantie, budget-energie, coolblue-energie, easyenergy, eneco, energie-vanons, energyzero, frank-energie, frank-energie-slim, hegg, innova, nextenergy, samsam, tibber, vandebron, zonneplan]
      price-interval: quarterly # Price interval, [hourly, quarterly]
    advanced: |
      type: template
      template: epexprijzen-nl
      provider: # Energy provider, [anwb-energie, atoom-alliantie, budget-energie, coolblue-energie, easyenergy, eneco, energie-vanons, energyzero, frank-energie, frank-energie-slim, hegg, innova, nextenergy, samsam, tibber, vandebron, zonneplan]
      price-interval: quarterly # Price interval, [hourly, quarterly]
      charges: # Charge, Additional fixed charge per kWh (e.g. 0.05 for 5 cents) (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
      interval: 1h # Interval, optional
params:
  - name: provider
    example:
    default:
    choice: ['anwb-energie', 'atoom-alliantie', 'budget-energie', 'coolblue-energie', 'easyenergy', 'eneco', 'energie-vanons', 'energyzero', 'frank-energie', 'frank-energie-slim', 'hegg', 'innova', 'nextenergy', 'samsam', 'tibber', 'vandebron', 'zonneplan']
    unit:
    description: Energy provider
    help:
    advanced: false
    optional: false
  - name: price-interval
    example:
    default: quarterly
    choice: ['hourly', 'quarterly']
    unit:
    description: Price interval
    help:
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh (e.g. 0.05 for 5 cents)
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true
  - name: interval
    example:
    default: 1h
    choice: []
    unit:
    description: Interval
    help:
    advanced: true
    optional: true