template: tinkerforge-warp2-ws
product:
  identifier: tinkerforge-warp2-smart-plus-energy-manager
  brand: TinkerForge
  description: WARP2 Smart + Energy Manager
capabilities: ["mA", "1p3p", "rfid"]
requirements: ["skiptest"]
description: |
  Firmware v2 required. Automatic phase switching requires the additional WARP Energy Manager.
render:
  - default: |
      type: template
      template: tinkerforge-warp2-ws
      uri: # URI, HTTP(S) address
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
      energyManagerUri: # Energy Manager URI, HTTP(S) address of the WARP Energy Manager (optional)
      energyManagerUser: # Energy Manager Username, e.g. email address, user id, etc. (optional)
      energyManagerPassword: # Energy Manager Password, Use single quotes in case of leading zeros (optional)
params:
  - name: uri
    example:
    default:
    choice: []
    unit:
    description: URI
    help: HTTP(S) address
    advanced: false
    optional: false
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: true
  - name: energyManagerUri
    example:
    default:
    choice: []
    unit:
    description: Energy Manager URI
    help: HTTP(S) address of the WARP Energy Manager
    advanced: false
    optional: true
  - name: energyManagerUser
    example:
    default:
    choice: []
    unit:
    description: Energy Manager Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: energyManagerPassword
    example:
    default:
    choice: []
    unit:
    description: Energy Manager Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: true