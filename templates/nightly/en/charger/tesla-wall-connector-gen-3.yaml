template: twc3
product:
  identifier: tesla-wall-connector-gen-3
  brand: Tesla
  description: Wall Connector (Gen 3)
description: |
  The TWC wallbox cannot be controlled directly. Control is via the vehicle. The vehicle must be selected at the TWC3 loadpoint. At this time only [Tesla vehicles](https://docs.evcc.io/en/docs/devices/vehicles#tesla) are supported.
render:
  - default: |
      type: template
      template: twc3
      host: 192.0.2.2 # IP address or hostname
params:
  - name: host
    example: 192.0.2.2
    default:
    choice: []
    unit:
    description: IP address or hostname
    help:
    advanced: false
    optional: false