template: hardybarth-salia
product:
  identifier: hardy-barth-cph2
  brand: Hardy Barth
  description: cPH2
requirements: ["sponsorship"]
description: |
  If you get the message '401 (Unauthorized)' without a username and password, use the username and password that you received from the installer of your wallbox. The combo that also works for the web interface of the wallbox.
render:
  - default: |
      type: template
      template: hardybarth-salia
      host: 192.0.2.2 # IP address or hostname
      user: # Username, Username (optional, only if Basic Auth is enabled on the wallbox) (optional)
      password: # Password, Password (optional, only if Basic Auth is enabled on the wallbox) (optional)
params:
  - name: host
    example: 192.0.2.2
    default:
    choice: []
    unit:
    description: IP address or hostname
    help:
    advanced: false
    optional: false
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: Username (optional, only if Basic Auth is enabled on the wallbox)
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Password (optional, only if Basic Auth is enabled on the wallbox)
    advanced: false
    optional: true