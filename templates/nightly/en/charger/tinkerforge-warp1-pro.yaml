template: tinkerforge-warp-ws
product:
  identifier: tinkerforge-warp1-pro
  brand: TinkerForge
  description: WARP1 Pro
capabilities: ["mA", "1p3p", "rfid"]
requirements: ["skiptest"]
description: |
  The automatic phase switching for 1p vehicles must be deactivated. See [docs.warp-charger.com](https://docs.warp-charger.com/docs/mqtt_http/api_reference/evse#evse_phase_auto_switch_warp3).
render:
  - default: |
      type: template
      template: tinkerforge-warp-ws
      uri: # URI, HTTP(S) address
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
    advanced: |
      type: template
      template: tinkerforge-warp-ws
      uri: # URI, HTTP(S) address
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
      energyMeterIndex: 0 # Energy Meter Index, Index of the meter configured in the WARP charger whose values should be used. (optional)
params:
  - name: uri
    example:
    default:
    choice: []
    unit:
    description: URI
    help: HTTP(S) address
    advanced: false
    optional: false
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: true
  - name: energyMeterIndex
    example:
    default: 0
    choice: []
    unit:
    description: Energy Meter Index
    help: Index of the meter configured in the WARP charger whose values should be used.
    advanced: true
    optional: true