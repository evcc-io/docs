template: enovates
product:
  identifier: shell-recharge-home-advanced
  brand: Shell Recharge
  description: Home Advanced
capabilities: ["mA", "meter"]
requirements: ["sponsorship"]
description: |
  EMS over Modbus TCP must be enabled on the charger (ModBusTCPEMSEnabled and modbus.tcp.enabled = true).
render:
  - default: |
      type: template
      template: enovates

      # Modbus TCP
      modbus: tcpip
      id: 1
      host:  # Hostname
      port: 502 # Port
params:
  - name: modbus
    example:
    default:
    choice: ['tcpip']
    unit:
    description: Modbus Type
    help:
    advanced: false
    optional: false
modbus:
  host:
  id: 1
  modbus: tcpip
  port: 502
  tcpip: true