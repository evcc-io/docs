product:
  brand: go-e
  description: Charger HOMEfix
capabilities: ["rfid"]
requirements: ["sponsorship"]
description: |
  Requires firmware 040.0 or later. HTTP API v1 or v2 must be activated.
render:
  - default: |
      type: template
      template: go-e
      host: 192.0.2.2 # IP address or hostname
