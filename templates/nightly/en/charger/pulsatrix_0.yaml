product:
  brand: Pulsatrix
requirements: ["sponsorship"]
render:
  - default: |
      type: template
      template: pulsatrix
      host: 192.0.2.2 # IP address or hostname, Shown on 3rd page of SECC display
