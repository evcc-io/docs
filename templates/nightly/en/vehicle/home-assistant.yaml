template: homeassistant
product:
  identifier: home-assistant
  brand: Home Assistant
  group: Generic support
description: |
  Home Assistant instances in your network will be auto-discovered and suitable vehicle entities and services (e.g. `sensor.*`) will be suggested.
render:
  - default: |
      type: template
      template: homeassistant
      uri: http://homeassistant.local:8123 # Home Assistant URI
      soc: sensor.vehicle_soc # State of charge [%], Entity ID for the vehicle's battery state of charge in percent
      title: # Title, Will be displayed in the user interface (optional)
      capacity: 50 # Battery capacity (kWh), optional
      range: sensor.vehicle_range # Remaining range [km], Entity ID for the vehicle's remaining range in kilometers (optional)
      status: sensor.vehicle_charging # Charging status, Entity ID for charging status (A=disconnected, B=connected, C=charging) (optional)
      limitSoc: number.vehicle_target_state_of_charge # Target state of charge [%], Entity ID for the vehicle's target state of charge in percent (`number` or `input_number` entity) (optional)
      odometer: sensor.vehicle_odometer # Odometer [km], Entity ID for the vehicle's odometer reading in kilometers (optional)
      climater: binary_sensor.vehicle_climater # Climatisation active, Entity ID for the vehicle's climatisation state (`binary_sensor` with `on`/`off` state) (optional)
      finishTime: sensor.vehicle_finish_time # Finish time, Entity ID for the estimated charging finish time (ISO8601 or Unix timestamp) (optional)
      start_charging: script.vehicle_start_charge; switch.vehicle_charger # Service to start charging, Entity ID for a script or a switch that starts charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger). If a switch is provided, Service to stop charging must be left empty. (optional)
      stop_charging: script.vehicle_stop_charge # Service to stop charging, Entity ID for a script that stops charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger). (optional)
      wakeup: script.vehicle_wakeup; button.wakeup_vehicle # Service to wake up vehicle, Entity ID for a script or a button that wakes up the vehicle (optional)
      setMaxCurrent: number.vehicle_charging_current # Set charging current [A], Entity ID for setting the maximum charging current in amperes (`number` or `input_number` entity) (optional)
    advanced: |
      type: template
      template: homeassistant
      uri: http://homeassistant.local:8123 # Home Assistant URI
      soc: sensor.vehicle_soc # State of charge [%], Entity ID for the vehicle's battery state of charge in percent
      title: # Title, Will be displayed in the user interface (optional)
      capacity: 50 # Battery capacity (kWh), optional
      range: sensor.vehicle_range # Remaining range [km], Entity ID for the vehicle's remaining range in kilometers (optional)
      status: sensor.vehicle_charging # Charging status, Entity ID for charging status (A=disconnected, B=connected, C=charging) (optional)
      limitSoc: number.vehicle_target_state_of_charge # Target state of charge [%], Entity ID for the vehicle's target state of charge in percent (`number` or `input_number` entity) (optional)
      odometer: sensor.vehicle_odometer # Odometer [km], Entity ID for the vehicle's odometer reading in kilometers (optional)
      climater: binary_sensor.vehicle_climater # Climatisation active, Entity ID for the vehicle's climatisation state (`binary_sensor` with `on`/`off` state) (optional)
      finishTime: sensor.vehicle_finish_time # Finish time, Entity ID for the estimated charging finish time (ISO8601 or Unix timestamp) (optional)
      start_charging: script.vehicle_start_charge; switch.vehicle_charger # Service to start charging, Entity ID for a script or a switch that starts charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger). If a switch is provided, Service to stop charging must be left empty. (optional)
      stop_charging: script.vehicle_stop_charge # Service to stop charging, Entity ID for a script that stops charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger). (optional)
      wakeup: script.vehicle_wakeup; button.wakeup_vehicle # Service to wake up vehicle, Entity ID for a script or a button that wakes up the vehicle (optional)
      setMaxCurrent: number.vehicle_charging_current # Set charging current [A], Entity ID for setting the maximum charging current in amperes (`number` or `input_number` entity) (optional)
      icon: car # Icon, Will be displayed in the user interface [car, bike, bus, moped, motorcycle, rocket, scooter, taxi, tractor, rickshaw, shuttle, van, airpurifier, battery, bulb, climate, coffeemaker, compute, cooking, cooler, desktop, device, dishwasher, dryer, floorlamp, generic, heater, heatexchange, heatpump, kettle, laundry, laundry2, machine, meter, microwave, pump, smartconsumer, tool, waterheater] (optional)
      phases: 3 # Maximum number of phases, The maximum number of phases which can be used (optional)
      mode: # Default charging mode, Used when a vehicle is connected. Possible values are Off, Now, MinPV and PV, or empty if none should be set (optional)
      minCurrent: 6 # Minimum amperage (A), The minimum amperage per connected phase that can be used (optional)
      maxCurrent: 16 # Maximum amperage (A), The maximum amperage per connected phase that can be used (optional)
      maxPower: # Maximum charging power hint (W), Defines the maximum charging power of the vehicle. Helps improve charge plan accuracy when the vehicle typically uses less than the offered current or supports higher single-phase current compared to three-phase. _The offered current of the loadpoint is not affected._ (optional)
      identifiers: # Identification, Mostly this can be added later, see: https://docs.evcc.io/en/docs/features/vehicle (optional)
      priority: # Priority, Priority of the loadpoint or vehicle in relation to other loadpoints or vehicles for allocating pv energy (optional)
      coarsecurrent: # 1A current control, Vehicle supports 1A current steps only (optional)
      streaming: true # Supports streaming, Streaming data is received asynchronously (optional)
      welcomecharge: # Charge on connection, Charger will enable charging for short time when vehicle is connected, irrespective of configured charge mode. This is useful for vehicles that require power supply when connecting. (optional)
params:
  - name: uri
    example: http://homeassistant.local:8123
    default:
    choice: []
    unit:
    description: Home Assistant URI
    help:
    advanced: false
    optional: false
  - name: soc
    example: sensor.vehicle_soc
    default:
    choice: []
    unit:
    description: State of charge [%]
    help: Entity ID for the vehicle's battery state of charge in percent
    advanced: false
    optional: false
  - name: title
    example:
    default:
    choice: []
    unit:
    description: Title
    help: Will be displayed in the user interface
    advanced: false
    optional: true
  - name: capacity
    example: 50
    default:
    choice: []
    unit: kWh
    description: Battery capacity
    help:
    advanced: false
    optional: true
  - name: range
    example: sensor.vehicle_range
    default:
    choice: []
    unit:
    description: Remaining range [km]
    help: Entity ID for the vehicle's remaining range in kilometers
    advanced: false
    optional: true
  - name: status
    example: sensor.vehicle_charging
    default:
    choice: []
    unit:
    description: Charging status
    help: Entity ID for charging status (A=disconnected, B=connected, C=charging)
    advanced: false
    optional: true
  - name: limitSoc
    example: number.vehicle_target_state_of_charge
    default:
    choice: []
    unit:
    description: Target state of charge [%]
    help: Entity ID for the vehicle's target state of charge in percent (`number` or `input_number` entity)
    advanced: false
    optional: true
  - name: odometer
    example: sensor.vehicle_odometer
    default:
    choice: []
    unit:
    description: Odometer [km]
    help: Entity ID for the vehicle's odometer reading in kilometers
    advanced: false
    optional: true
  - name: climater
    example: binary_sensor.vehicle_climater
    default:
    choice: []
    unit:
    description: Climatisation active
    help: Entity ID for the vehicle's climatisation state (`binary_sensor` with `on`/`off` state)
    advanced: false
    optional: true
  - name: finishTime
    example: sensor.vehicle_finish_time
    default:
    choice: []
    unit:
    description: Finish time
    help: Entity ID for the estimated charging finish time (ISO8601 or Unix timestamp)
    advanced: false
    optional: true
  - name: start_charging
    example: script.vehicle_start_charge; switch.vehicle_charger
    default:
    choice: []
    unit:
    description: Service to start charging
    help: Entity ID for a script or a switch that starts charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger). If a switch is provided, Service to stop charging must be left empty.
    advanced: false
    optional: true
  - name: stop_charging
    example: script.vehicle_stop_charge
    default:
    choice: []
    unit:
    description: Service to stop charging
    help: Entity ID for a script that stops charging the vehicle. Only useful with the [Vehicle API-only charger](https://docs.evcc.io/en/docs/devices/chargers#vehicle-api-only-charger).
    advanced: false
    optional: true
  - name: wakeup
    example: script.vehicle_wakeup; button.wakeup_vehicle
    default:
    choice: []
    unit:
    description: Service to wake up vehicle
    help: Entity ID for a script or a button that wakes up the vehicle
    advanced: false
    optional: true
  - name: setMaxCurrent
    example: number.vehicle_charging_current
    default:
    choice: []
    unit:
    description: Set charging current [A]
    help: Entity ID for setting the maximum charging current in amperes (`number` or `input_number` entity)
    advanced: false
    optional: true
  - name: icon
    example:
    default: car
    choice: ['car', 'bike', 'bus', 'moped', 'motorcycle', 'rocket', 'scooter', 'taxi', 'tractor', 'rickshaw', 'shuttle', 'van', 'airpurifier', 'battery', 'bulb', 'climate', 'coffeemaker', 'compute', 'cooking', 'cooler', 'desktop', 'device', 'dishwasher', 'dryer', 'floorlamp', 'generic', 'heater', 'heatexchange', 'heatpump', 'kettle', 'laundry', 'laundry2', 'machine', 'meter', 'microwave', 'pump', 'smartconsumer', 'tool', 'waterheater']
    unit:
    description: Icon
    help: Will be displayed in the user interface
    advanced: true
    optional: true
  - name: phases
    example: 3
    default:
    choice: []
    unit:
    description: Maximum number of phases
    help: The maximum number of phases which can be used
    advanced: true
    optional: true
  - name: mode
    example:
    default:
    choice: []
    unit:
    description: Default charging mode
    help: Used when a vehicle is connected. Possible values are Off, Now, MinPV and PV, or empty if none should be set
    advanced: true
    optional: true
  - name: minCurrent
    example: 6
    default:
    choice: []
    unit: A
    description: Minimum amperage
    help: The minimum amperage per connected phase that can be used
    advanced: true
    optional: true
  - name: maxCurrent
    example: 16
    default:
    choice: []
    unit: A
    description: Maximum amperage
    help: The maximum amperage per connected phase that can be used
    advanced: true
    optional: true
  - name: maxPower
    example:
    default:
    choice: []
    unit: W
    description: Maximum charging power hint
    help: Defines the maximum charging power of the vehicle. Helps improve charge plan accuracy when the vehicle typically uses less than the offered current or supports higher single-phase current compared to three-phase. _The offered current of the loadpoint is not affected._
    advanced: true
    optional: true
  - name: identifiers
    example:
    default:
    choice: []
    unit:
    description: Identification
    help: 'Mostly this can be added later, see: https://docs.evcc.io/en/docs/features/vehicle'
    advanced: true
    optional: true
  - name: priority
    example:
    default:
    choice: []
    unit:
    description: Priority
    help: Priority of the loadpoint or vehicle in relation to other loadpoints or vehicles for allocating pv energy
    advanced: true
    optional: true
  - name: coarsecurrent
    example:
    default:
    choice: []
    unit:
    description: 1A current control
    help: Vehicle supports 1A current steps only
    advanced: true
    optional: true
  - name: streaming
    example:
    default: true
    choice: []
    unit:
    description: Supports streaming
    help: Streaming data is received asynchronously
    advanced: true
    optional: true
  - name: welcomecharge
    example:
    default:
    choice: []
    unit:
    description: Charge on connection
    help: Charger will enable charging for short time when vehicle is connected, irrespective of configured charge mode. This is useful for vehicles that require power supply when connecting.
    advanced: true
    optional: true