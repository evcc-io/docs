product:
  brand: Bosch
  description: BPT-S 5 Hybrid
requirements: ["skiptest"]
render:
  - usage: grid
    default: |
      type: template
      template: bosch-bpt
      usage: grid
      uri: # HTTP(S) Adresse (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: grid
      uri: # HTTP(S) Adresse (optional)
  - usage: pv
    default: |
      type: template
      template: bosch-bpt
      usage: pv
      uri: # HTTP(S) Adresse (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: pv
      uri: # HTTP(S) Adresse (optional)
  - usage: battery
    default: |
      type: template
      template: bosch-bpt
      usage: battery
      uri: # HTTP(S) Adresse (optional)
    advanced: |
      type: template
      template: bosch-bpt
      usage: battery
      uri: # HTTP(S) Adresse (optional)
      capacity: 50 # optional
