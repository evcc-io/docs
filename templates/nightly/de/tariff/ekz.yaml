template: ekz
product:
  identifier: ekz
  brand: EKZ
  group: Dynamischer Strompreis
countries: ["CH"]
requirements: ["skiptest"]
render:
  - default: |
      type: template
      template: ekz
      tariff_name: 400D # Desired tariff (400D for dynamic)., [400D, 400F, 400ST, 400WP, 400L, 400LS, 16L, 16LS]
    advanced: |
      type: template
      template: ekz
      tariff_name: 400D # Desired tariff (400D for dynamic)., [400D, 400F, 400ST, 400WP, 400L, 400LS, 16L, 16LS]
      charges: # Aufschlag, Zusätzlicher fester Aufschlag pro kWh (z.B. 0.05 für 5 Cent) (optional)
      tax: 0.081 # Steuer, Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formel, Individuelle Formel zur Berechnung des Preises (optional)
      interval: 1h # Intervall, optional
params:
  - name: tariff_name
    example: 400D
    default: 400D
    choice: ['400D', '400F', '400ST', '400WP', '400L', '400LS', '16L', '16LS']
    unit:
    description: Desired tariff (400D for dynamic).
    help:
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Aufschlag
    help: Zusätzlicher fester Aufschlag pro kWh (z.B. 0.05 für 5 Cent)
    advanced: true
    optional: true
  - name: tax
    example:
    default: 0.081
    choice: []
    unit:
    description: Steuer
    help: Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formel
    help: Individuelle Formel zur Berechnung des Preises
    advanced: true
    optional: true
  - name: interval
    example:
    default: 1h
    choice: []
    unit:
    description: Intervall
    help:
    advanced: true
    optional: true