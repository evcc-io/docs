template: ckw
product:
  identifier: ckw-netz-home-business-dynamic
  brand: CKW
  description: Netz Home/Business dynamic
  group: Dynamischer Strompreis
countries: ["CH"]
requirements: ["skiptest"]
render:
  - default: |
      type: template
      template: ckw
      tariff: # Tarifname, home_dynamic (unter 50 MWh/Jahr) oder business_dynamic (über 50 MWh/Jahr) [home_dynamic, business_dynamic]
    advanced: |
      type: template
      template: ckw
      tariff: # Tarifname, home_dynamic (unter 50 MWh/Jahr) oder business_dynamic (über 50 MWh/Jahr) [home_dynamic, business_dynamic]
      charges: # Aufschlag, Zusätzlicher fester Aufschlag pro kWh (z.B. 0.05 für 5 Cent) (optional)
      tax: 0.081 # Steuer, Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formel, Individuelle Formel zur Berechnung des Preises (optional)
params:
  - name: tariff
    example:
    default:
    choice: ['home_dynamic', 'business_dynamic']
    unit:
    description: Tarifname
    help: home_dynamic (unter 50 MWh/Jahr) oder business_dynamic (über 50 MWh/Jahr)
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Aufschlag
    help: Zusätzlicher fester Aufschlag pro kWh (z.B. 0.05 für 5 Cent)
    advanced: true
    optional: true
  - name: tax
    example:
    default: 0.081
    choice: []
    unit:
    description: Steuer
    help: Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formel
    help: Individuelle Formel zur Berechnung des Preises
    advanced: true
    optional: true