template: email
product:
  identifier: email
  description: Email
render:
  - default: |
      type: template
      template: email
      host: smtp.example.com # IP address or hostname
      port: 465 # Port
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Use single quotes in case of leading zeros
      from: john.doe@example.com # From, Sender's email address.
      to: # To, Email addresses of recipients. Ein Eintrag pro Zeile.
params:
  - name: host
    example: smtp.example.com
    default:
    choice: []
    unit:
    description: IP address or hostname
    help:
    advanced: false
    optional: false
  - name: port
    example: 465
    default: 465
    choice: []
    unit:
    description: Port
    help:
    advanced: false
    optional: false
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: false
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: false
  - name: from
    example: john.doe@example.com
    default:
    choice: []
    unit:
    description: From
    help: Sender's email address.
    advanced: false
    optional: false
  - name: to
    example: recipient@example.com
    default:
    choice: []
    unit:
    description: To
    help: Email addresses of recipients. Ein Eintrag pro Zeile.
    advanced: false
    optional: false