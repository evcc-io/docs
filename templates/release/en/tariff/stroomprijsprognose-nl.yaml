template: stroomprijsprognose
product:
  identifier: stroomprijsprognose-nl
  brand: stroomprijsprognose.nl
  group: Dynamic electricity price
countries: ["DE", "NL", "BE", "DK", "FR"]
requirements: ["skiptest"]
description: |
  Spot prices in day-ahead market plus 4 days prognosis [stroomprijsprognose.nl](https://stroomprijsprognose.nl)
render:
  - default: |
      type: template
      template: stroomprijsprognose
      region: DE # Region, [DE, NL, BE, DK1, DK2, FR]
      currency: EUR # Currency, [EUR, DKK]
      horizon: 120 # Forecast horizon (h)
    advanced: |
      type: template
      template: stroomprijsprognose
      region: DE # Region, [DE, NL, BE, DK1, DK2, FR]
      currency: EUR # Currency, [EUR, DKK]
      horizon: 120 # Forecast horizon (h)
      charges: # Charge, Additional fixed charge per kWh (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
      average: # Average by hour, optional
params:
  - name: region
    example: DE
    default:
    choice: ['DE', 'NL', 'BE', 'DK1', 'DK2', 'FR']
    unit:
    description: Region
    help:
    advanced: false
    optional: false
  - name: currency
    example:
    default: EUR
    choice: ['EUR', 'DKK']
    unit:
    description: Currency
    help:
    advanced: false
    optional: false
  - name: horizon
    example:
    default: 120
    choice: []
    unit: h
    description: Forecast horizon
    help:
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true
  - name: average
    example:
    default:
    choice: []
    unit:
    description: Average by hour
    help:
    advanced: true
    optional: true