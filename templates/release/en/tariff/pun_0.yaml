product:
  brand: PUN Orario (IT)
render:
  - default: |
      type: template
      template: pun
    advanced: |
      type: template
      template: pun
      costs: # optional
      tax: # optional
