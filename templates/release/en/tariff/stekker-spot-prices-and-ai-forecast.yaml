template: stekker
product:
  identifier: stekker-spot-prices-and-ai-forecast
  brand: Stekker spot prices and AI Forecast
  group: Dynamic electricity price
countries: ["EU"]
requirements: ["skiptest"]
description: |
  Stekker.app spot prices in day-ahead market and 24h AI price Forecast
render:
  - default: |
      type: template
      template: stekker
      region: BE # Market region code, [DE-LU, EE, LT, LV, AT, BE, FR, GER, NL, PL, DK1, DK2, FI, NO1, NO2, NO3, NO4, NO5, SE1, SE2, SE3, SE4, TEL, SYS, CH, RO, PT, RS, SI, SK, HU, CZ, HR]
    advanced: |
      type: template
      template: stekker
      region: BE # Market region code, [DE-LU, EE, LT, LV, AT, BE, FR, GER, NL, PL, DK1, DK2, FI, NO1, NO2, NO3, NO4, NO5, SE1, SE2, SE3, SE4, TEL, SYS, CH, RO, PT, RS, SI, SK, HU, CZ, HR]
      charges: # Charge, Additional fixed charge per kWh (e.g. 0.05 for 5 cents) (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
      average: # Average by hour, optional
params:
  - name: region
    example: BE
    default:
    choice: ['DE-LU', 'EE', 'LT', 'LV', 'AT', 'BE', 'FR', 'GER', 'NL', 'PL', 'DK1', 'DK2', 'FI', 'NO1', 'NO2', 'NO3', 'NO4', 'NO5', 'SE1', 'SE2', 'SE3', 'SE4', 'TEL', 'SYS', 'CH', 'RO', 'PT', 'RS', 'SI', 'SK', 'HU', 'CZ', 'HR']
    unit:
    description: Market region code
    help:
    advanced: false
    optional: false
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh (e.g. 0.05 for 5 cents)
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true
  - name: average
    example:
    default:
    choice: []
    unit:
    description: Average by hour
    help:
    advanced: true
    optional: true