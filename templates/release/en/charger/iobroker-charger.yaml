template: iobroker
product:
  identifier: iobroker-charger
  brand: ioBroker
  description: Charger
  group: Generic support
requirements: ["skiptest"]
description: |
  The `rest-api` adapter (not `simple-api`) must be installed on the ioBroker instance being used.
  Authentication can only be performed using `basic` authentication. If no password is provided, no authentication will be performed.
  When specifying the entities for the individual measured values, ensure that they are correctly URL-encoded.
  For example, `shelly.0.SHEM-3#8CAAB561991E#1.Total.ConsumedPower` becomes `shelly.0.SHEM-3%238CAAB561991E%231.Total.ConsumedPower`

render:
  - default: |
      type: template
      template: iobroker
      uri: https://iobroker.homelab.lan:8082 # The full uri of the host to query, HTTP(S) address
      status: # Charge status (A, B, C)
      enabled: # Enabled
      enable: # Set enabled
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Service account password (optional)
      power: # Power (W), optional
      maxcurrent: 16 # Set maximum amperage (A), The maximum current per connected phase that can be used (optional)
      phases1p3p: # Set num phases (1 or 3), optional
    advanced: |
      type: template
      template: iobroker
      uri: https://iobroker.homelab.lan:8082 # The full uri of the host to query, HTTP(S) address
      status: # Charge status (A, B, C)
      enabled: # Enabled
      enable: # Set enabled
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Service account password (optional)
      power: # Power (W), optional
      maxcurrent: 16 # Set maximum amperage (A), The maximum current per connected phase that can be used (optional)
      phases1p3p: # Set num phases (1 or 3), optional
      cache: 5m # Cache, Time interval for data refresh (optional)
      maxcurrentmillis: # Set maximum amperage (mA), optional
params:
  - name: uri
    example: https://iobroker.homelab.lan:8082
    default:
    choice: []
    unit:
    description: The full uri of the host to query
    help: HTTP(S) address
    advanced: false
    optional: false
  - name: status
    example:
    default:
    choice: []
    unit:
    description: Charge status (A, B, C)
    help:
    advanced: false
    optional: false
  - name: enabled
    example:
    default:
    choice: []
    unit:
    description: Enabled
    help:
    advanced: false
    optional: false
  - name: enable
    example:
    default:
    choice: []
    unit:
    description: Set enabled
    help:
    advanced: false
    optional: false
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Service account password
    advanced: false
    optional: true
  - name: power
    example:
    default:
    choice: []
    unit: W
    description: Power
    help:
    advanced: false
    optional: true
  - name: maxcurrent
    example: 16
    default:
    choice: []
    unit: A
    description: Set maximum amperage
    help: The maximum current per connected phase that can be used
    advanced: false
    optional: true
  - name: phases1p3p
    example:
    default:
    choice: []
    unit:
    description: Set num phases (1 or 3)
    help:
    advanced: false
    optional: true
  - name: cache
    example: 5m
    default:
    choice: []
    unit:
    description: Cache
    help: Time interval for data refresh
    advanced: true
    optional: true
  - name: maxcurrentmillis
    example:
    default:
    choice: []
    unit: mA
    description: Set maximum amperage
    help:
    advanced: true
    optional: true