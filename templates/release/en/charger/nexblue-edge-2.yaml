template: nexblue
product:
  identifier: nexblue-edge-2
  brand: Nexblue
  description: Edge 2
requirements: ["sponsorship"]
render:
  - default: |
      type: template
      template: nexblue
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
      serial: # Serial, Charger serial number (auto-detected if only one charger in account) (optional)
params:
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: true
  - name: serial
    example:
    default:
    choice: []
    unit:
    description: Serial
    help: Charger serial number (auto-detected if only one charger in account)
    advanced: false
    optional: true