template: senec-connect
product:
  identifier: senec-connect-cloud
  brand: SENEC
  description: .Connect (Cloud)
description: |
  Cloud connection via the SENEC.Connect API. A subscription key from the SENEC developer portal is required. Access is read-only, battery control is not possible.
render:
  - usage: grid
    default: |
      type: template
      template: senec-connect
      usage: grid
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
    advanced: |
      type: template
      template: senec-connect
      usage: grid
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
  - usage: pv
    default: |
      type: template
      template: senec-connect
      usage: pv
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
    advanced: |
      type: template
      template: senec-connect
      usage: pv
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
      maxacpower: 0 # Maximum AC power of the hybrid inverter (W), optional
  - usage: battery
    default: |
      type: template
      template: senec-connect
      usage: battery
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
      minsoc: 25 # Minimum charge (%), Lower limit when discharging the battery in normal operation (optional)
      maxsoc: 95 # Maximum charge (%), Upper limit when charging the battery from the grid (optional)
    advanced: |
      type: template
      template: senec-connect
      usage: battery
      token: # Subscription key, Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
      minsoc: 25 # Minimum charge (%), Lower limit when discharging the battery in normal operation (optional)
      maxsoc: 95 # Maximum charge (%), Upper limit when charging the battery from the grid (optional)
params:
  - name: token
    example:
    default:
    choice: []
    unit:
    description: Subscription key
    help: Subscription key from the SENEC developer portal (sent as header `Ocp-Apim-Subscription-Key`).
    advanced: false
    optional: false
  - name: minsoc
    example: 25
    default:
    choice: []
    unit: '%'
    description: Minimum charge
    help: Lower limit when discharging the battery in normal operation
    advanced: false
    optional: true
  - name: maxsoc
    example: 95
    default:
    choice: []
    unit: '%'
    description: Maximum charge
    help: Upper limit when charging the battery from the grid
    advanced: false
    optional: true
  - name: maxacpower
    example: 5000
    default: 0
    choice: []
    unit: W
    description: Maximum AC power of the hybrid inverter
    help:
    advanced: true
    optional: true