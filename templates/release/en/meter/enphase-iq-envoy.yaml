template: enphase
product:
  identifier: enphase-iq-envoy
  brand: Enphase
  description: IQ Envoy
render:
  - usage: grid
    default: |
      type: template
      template: enphase
      usage: grid
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
    advanced: |
      type: template
      template: enphase
      usage: grid
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
      schema: http # Schema, [https, http] (optional)
      cache: 1s # Cache, Time interval for data refresh (optional)
      battery_type: ac # Enphase Battery Type (AC or IQ), Select 'iq' for new Enphase IQ Batteries. Leave at default 'ac' for older generation AC Batteries. [ac, iq] (optional)
  - usage: pv
    default: |
      type: template
      template: enphase
      usage: pv
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
    advanced: |
      type: template
      template: enphase
      usage: pv
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
      schema: http # Schema, [https, http] (optional)
      cache: 1s # Cache, Time interval for data refresh (optional)
      battery_type: ac # Enphase Battery Type (AC or IQ), Select 'iq' for new Enphase IQ Batteries. Leave at default 'ac' for older generation AC Batteries. [ac, iq] (optional)
  - usage: battery
    default: |
      type: template
      template: enphase
      usage: battery
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
    advanced: |
      type: template
      template: enphase
      usage: battery
      host: 192.0.2.2 # IP address or hostname
      token: # Token, Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication (optional)
      schema: http # Schema, [https, http] (optional)
      capacity: 50 # Battery capacity (kWh), optional
      cache: 1s # Cache, Time interval for data refresh (optional)
      battery_type: ac # Enphase Battery Type (AC or IQ), Select 'iq' for new Enphase IQ Batteries. Leave at default 'ac' for older generation AC Batteries. [ac, iq] (optional)
params:
  - name: host
    example: 192.0.2.2
    default:
    choice: []
    unit:
    description: IP address or hostname
    help:
    advanced: false
    optional: false
  - name: token
    example:
    default:
    choice: []
    unit:
    description: Token
    help: 'Required if Envoy Firmware D7.x.xxx. Token is valid for one year. Instructions for obtaining a token via web UI: https://enphase.com/download/accessing-iq-gateway-local-apis-or-local-ui-token-based-authentication'
    advanced: false
    optional: true
  - name: schema
    example:
    default: http
    choice: ['https', 'http']
    unit:
    description: Schema
    help:
    advanced: true
    optional: true
  - name: capacity
    example: 50
    default:
    choice: []
    unit: kWh
    description: Battery capacity
    help:
    advanced: true
    optional: true
  - name: cache
    example: 5m
    default: 1s
    choice: []
    unit:
    description: Cache
    help: Time interval for data refresh
    advanced: true
    optional: true
  - name: battery_type
    example:
    default: ac
    choice: ['ac', 'iq']
    unit:
    description: Enphase Battery Type (AC or IQ)
    help: Select 'iq' for new Enphase IQ Batteries. Leave at default 'ac' for older generation AC Batteries.
    advanced: true
    optional: true