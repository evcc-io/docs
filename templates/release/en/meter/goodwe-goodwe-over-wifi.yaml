template: goodwe-wifi
product:
  identifier: goodwe-goodwe-over-wifi
  brand: GoodWe
  description: GoodWe over Wifi
requirements: ["skiptest"]
render:
  - usage: grid
    default: |
      type: template
      template: goodwe-wifi
      usage: grid
      uri: # IP address or hostname, HTTP(S) address (optional)
  - usage: pv
    default: |
      type: template
      template: goodwe-wifi
      usage: pv
      uri: # IP address or hostname, HTTP(S) address (optional)
  - usage: battery
    default: |
      type: template
      template: goodwe-wifi
      usage: battery
      uri: # IP address or hostname, HTTP(S) address (optional)
params:
  - name: uri
    example:
    default:
    choice: []
    unit:
    description: IP address or hostname
    help: HTTP(S) address
    advanced: false
    optional: true