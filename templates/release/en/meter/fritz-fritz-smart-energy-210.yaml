template: fritzdect
product:
  identifier: fritz-fritz-smart-energy-210
  brand: FRITZ!
  description: FRITZ!Smart Energy 210
  group: Switchable sockets
render:
  - usage: pv
    default: |
      type: template
      template: fritzdect
      usage: pv
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Service account password
      ain: 307788992233 # Actor Identification Number (AIN), Printed on the type label on the back of the device.
      uri: https://fritz.box # URI, HTTP(S) address (optional)
    advanced: |
      type: template
      template: fritzdect
      usage: pv
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Service account password
      ain: 307788992233 # Actor Identification Number (AIN), Printed on the type label on the back of the device.
      uri: https://fritz.box # URI, HTTP(S) address (optional)
      firmware82: # Use new REST API (FritzOS 8.2+), Use the new REST API for FritzOS version 8.2 and later (optional)
      unit: # Unit, Unit index for multi-unit devices (REST API only) (optional)
  - usage: charge
    default: |
      type: template
      template: fritzdect
      usage: charge
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Service account password
      ain: 307788992233 # Actor Identification Number (AIN), Printed on the type label on the back of the device.
      uri: https://fritz.box # URI, HTTP(S) address (optional)
    advanced: |
      type: template
      template: fritzdect
      usage: charge
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Service account password
      ain: 307788992233 # Actor Identification Number (AIN), Printed on the type label on the back of the device.
      uri: https://fritz.box # URI, HTTP(S) address (optional)
      firmware82: # Use new REST API (FritzOS 8.2+), Use the new REST API for FritzOS version 8.2 and later (optional)
      unit: # Unit, Unit index for multi-unit devices (REST API only) (optional)
params:
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: false
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Service account password
    advanced: false
    optional: false
  - name: ain
    example: 307788992233
    default:
    choice: []
    unit:
    description: Actor Identification Number (AIN)
    help: Printed on the type label on the back of the device.
    advanced: false
    optional: false
  - name: uri
    example:
    default: https://fritz.box
    choice: []
    unit:
    description: URI
    help: HTTP(S) address
    advanced: false
    optional: true
  - name: firmware82
    example:
    default:
    choice: []
    unit:
    description: Use new REST API (FritzOS 8.2+)
    help: Use the new REST API for FritzOS version 8.2 and later
    advanced: true
    optional: true
  - name: unit
    example:
    default:
    choice: []
    unit:
    description: Unit
    help: Unit index for multi-unit devices (REST API only)
    advanced: true
    optional: true