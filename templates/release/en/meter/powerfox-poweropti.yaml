template: powerfox-poweropti
product:
  identifier: powerfox-poweropti
  brand: Powerfox
  description: Poweropti
render:
  - usage: grid
    default: |
      type: template
      template: powerfox-poweropti
      usage: grid
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Use single quotes in case of leading zeros
    advanced: |
      type: template
      template: powerfox-poweropti
      usage: grid
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Use single quotes in case of leading zeros
      id: main # Id in case of multiple PowerOpti
  - usage: pv
    default: |
      type: template
      template: powerfox-poweropti
      usage: pv
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Use single quotes in case of leading zeros
    advanced: |
      type: template
      template: powerfox-poweropti
      usage: pv
      user: # Username, e.g. email address, user id, etc.
      password: # Password, Use single quotes in case of leading zeros
      id: main # Id in case of multiple PowerOpti
params:
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: false
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: false
  - name: id
    example:
    default: main
    choice: []
    unit:
    description:
    help: Id in case of multiple PowerOpti
    advanced: true
    optional: false