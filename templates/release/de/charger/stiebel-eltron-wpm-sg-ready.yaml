template: stiebel-wpm
product:
  identifier: stiebel-eltron-wpm-sg-ready
  brand: Stiebel Eltron
  description: WPM (SG Ready)
  group: Wärmeerzeuger
capabilities: ["meter", "dim"]
render:
  - default: |
      type: template
      template: stiebel-wpm

      # Modbus TCP
      modbus: tcpip
      id: 1
      host:  # Hostname
      port: 502 # Port
      tempsource: # Temperaturquelle, [warmwater, buffer] (optional)
    advanced: |
      type: template
      template: stiebel-wpm

      # Modbus TCP
      modbus: tcpip
      id: 1
      host:  # Hostname
      port: 502 # Port
      tempsource: # Temperaturquelle, [warmwater, buffer] (optional)
      power: # Leistungsaufnahme auslesen, Aufnahmeleistung des Inverters. Nur bei WPMsystem (WPM 4) verfügbar, nicht bei WPM 3/3i. (optional)
params:
  - name: modbus
    example:
    default:
    choice: ['tcpip']
    unit:
    description: Modbus Typ
    help:
    advanced: false
    optional: false
  - name: tempsource
    example:
    default:
    choice: ['warmwater', 'buffer']
    unit:
    description: Temperaturquelle
    help:
    advanced: false
    optional: true
  - name: power
    example:
    default:
    choice: []
    unit:
    description: Leistungsaufnahme auslesen
    help: Aufnahmeleistung des Inverters. Nur bei WPMsystem (WPM 4) verfügbar, nicht bei WPM 3/3i.
    advanced: true
    optional: true
modbus:
  delay:
  host:
  id: 1
  modbus: tcpip
  port: 502
  tcpip: true
  timeout: