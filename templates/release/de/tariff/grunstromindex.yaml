template: grünstromindex
product:
  identifier: grunstromindex
  brand: Grünstromindex
  group: CO₂ Vorhersage
countries: ["DE"]
requirements: ["skiptest"]
description: |
  Regionale Emissionsdaten von https://gruenstromindex.de
render:
  - default: |
      type: template
      template: grünstromindex
      zip: # Postleitzahl
      token: # Token, Token für den Zugriff auf die API von https://console.corrently.io/ (optional)
params:
  - name: zip
    example:
    default:
    choice: []
    unit:
    description: Postleitzahl
    help:
    advanced: false
    optional: false
  - name: token
    example:
    default:
    choice: []
    unit:
    description: Token
    help: Token für den Zugriff auf die API von https://console.corrently.io/
    advanced: false
    optional: true