template: epex-predictor
product:
  identifier: epex-predictor-predicted-epex-spot-prices
  brand: EPEX Predictor
  description: Predicted EPEX spot prices
  group: Dynamischer Strompreis
countries: ["DE", "AT", "NL", "BE", "SE", "DK"]
requirements: ["skiptest"]
description: |
  EPEX Spot Preisvorhersagen von epexpredictor.batzill.com
render:
  - default: |
      type: template
      template: epex-predictor
      region: DE # Region, [DE, AT, NL, BE, SE1, SE2, SE3, SE4, DK1, DK2]
    advanced: |
      type: template
      template: epex-predictor
      region: DE # Region, [DE, AT, NL, BE, SE1, SE2, SE3, SE4, DK1, DK2]
      uri: https://epexpredictor.batzill.com # API-Endpunkt (für selbst gehostete Instanzen), HTTP(S) Adresse (optional)
      charges: # Aufschlag, Zusätzlicher fester Aufschlag pro kWh (optional)
      tax: # Steuer, Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formel, Individuelle Formel zur Berechnung des Preises (optional)
      average: # Stündliche Durchschnittskosten verwenden, optional
params:
  - name: region
    example: DE
    default:
    choice: ['DE', 'AT', 'NL', 'BE', 'SE1', 'SE2', 'SE3', 'SE4', 'DK1', 'DK2']
    unit:
    description: Region
    help:
    advanced: false
    optional: false
  - name: uri
    example:
    default: https://epexpredictor.batzill.com
    choice: []
    unit:
    description: API-Endpunkt (für selbst gehostete Instanzen)
    help: HTTP(S) Adresse
    advanced: true
    optional: true
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Aufschlag
    help: Zusätzlicher fester Aufschlag pro kWh
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Steuer
    help: Zusätzlicher prozentualer Aufschlag (z.B. 0.2 für 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formel
    help: Individuelle Formel zur Berechnung des Preises
    advanced: true
    optional: true
  - name: average
    example:
    default:
    choice: []
    unit:
    description: Stündliche Durchschnittskosten verwenden
    help:
    advanced: true
    optional: true