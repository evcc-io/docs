template: voltcast
product:
  identifier: voltcast
  brand: Voltcast
  group: Dynamic electricity price
countries: ["EU"]
requirements: ["skiptest"]
description: |
  Requires a Voltcast API key — free DE-LU tier, paid any-zone tiers at [voltcast.com](https://voltcast.com/register). Forecast accuracy is published daily at [voltcast.com/accuracy](https://voltcast.com/accuracy).
render:
  - default: |
      type: template
      template: voltcast
      apikey: # API Key, API key from https://voltcast.com/dashboard
      zone: DE-LU # Bidding zone, [AL, AT, BE, BG, CH, CZ, DE-LU, DK1, DK2, EE, ES, FI, FR, GR, HR, HU, IE-SEM, IT-CALA, IT-CNOR, IT-CSUD, IT-NORD, IT-SARD, IT-SICI, IT-SUD, LT, LV, ME, MK, NL, NO1, NO2, NO3, NO4, NO5, PL, PT, RO, RS, SE1, SE2, SE3, SE4, SI, SK, UA, XK] (optional)
    advanced: |
      type: template
      template: voltcast
      apikey: # API Key, API key from https://voltcast.com/dashboard
      zone: DE-LU # Bidding zone, [AL, AT, BE, BG, CH, CZ, DE-LU, DK1, DK2, EE, ES, FI, FR, GR, HR, HU, IE-SEM, IT-CALA, IT-CNOR, IT-CSUD, IT-NORD, IT-SARD, IT-SICI, IT-SUD, LT, LV, ME, MK, NL, NO1, NO2, NO3, NO4, NO5, PL, PT, RO, RS, SE1, SE2, SE3, SE4, SI, SK, UA, XK] (optional)
      charges: # Charge, Additional fixed charge per kWh (optional)
      chargesZones: # Charges zones, Time-based grid fees that override the default charge. (optional)
      tax: # Tax, Additional percentage charge (e.g. 0.2 for 20%) (optional)
      formula: math.Max((price + charges) * (1 + tax), 0.0) # Formula, Individual formula for calculating the price (optional)
      average: # Average by hour, optional
params:
  - name: apikey
    example:
    default:
    choice: []
    unit:
    description: API Key
    help: API key from https://voltcast.com/dashboard
    advanced: false
    optional: false
  - name: zone
    example: DE-LU
    default: DE-LU
    choice: ['AL', 'AT', 'BE', 'BG', 'CH', 'CZ', 'DE-LU', 'DK1', 'DK2', 'EE', 'ES', 'FI', 'FR', 'GR', 'HR', 'HU', 'IE-SEM', 'IT-CALA', 'IT-CNOR', 'IT-CSUD', 'IT-NORD', 'IT-SARD', 'IT-SICI', 'IT-SUD', 'LT', 'LV', 'ME', 'MK', 'NL', 'NO1', 'NO2', 'NO3', 'NO4', 'NO5', 'PL', 'PT', 'RO', 'RS', 'SE1', 'SE2', 'SE3', 'SE4', 'SI', 'SK', 'UA', 'XK']
    unit:
    description: Bidding zone
    help:
    advanced: false
    optional: true
  - name: charges
    example:
    default:
    choice: []
    unit:
    description: Charge
    help: Additional fixed charge per kWh
    advanced: true
    optional: true
  - name: chargesZones
    example:
    default:
    choice: []
    unit:
    description: Charges zones
    help: Time-based grid fees that override the default charge.
    advanced: true
    optional: true
  - name: tax
    example:
    default:
    choice: []
    unit:
    description: Tax
    help: Additional percentage charge (e.g. 0.2 for 20%)
    advanced: true
    optional: true
  - name: formula
    example: math.Max((price + charges) * (1 + tax), 0.0)
    default:
    choice: []
    unit:
    description: Formula
    help: Individual formula for calculating the price
    advanced: true
    optional: true
  - name: average
    example:
    default:
    choice: []
    unit:
    description: Average by hour
    help:
    advanced: true
    optional: true