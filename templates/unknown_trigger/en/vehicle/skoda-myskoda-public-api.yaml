template: myskoda
product:
  identifier: skoda-myskoda-public-api
  brand: Škoda
  description: MyŠkoda (Public API)
description: |
  Request an API key at [go.skoda.eu/api-keys](https://go.skoda.eu/api-keys).
render:
  - default: |
      type: template
      template: myskoda
      apikey: # API Key, Request an API key at [go.skoda.eu/api-keys](https://go.skoda.eu/api-keys).
      vin: W... # Vehicle Identification Number, If you own multiple vehicles from the same manufacturer
      title: # Title, Will be displayed in the user interface (optional)
      capacity: 50 # Battery capacity (kWh), optional
      timeout: 10s # Timeout, optional
    advanced: |
      type: template
      template: myskoda
      apikey: # API Key, Request an API key at [go.skoda.eu/api-keys](https://go.skoda.eu/api-keys).
      vin: W... # Vehicle Identification Number, If you own multiple vehicles from the same manufacturer
      title: # Title, Will be displayed in the user interface (optional)
      capacity: 50 # Battery capacity (kWh), optional
      timeout: 10s # Timeout, optional
      icon: car # Icon, Will be displayed in the user interface [car, bike, bus, moped, motorcycle, rocket, scooter, taxi, tractor, rickshaw, shuttle, van, airpurifier, battery, bulb, climate, coffeemaker, compute, cooking, cooler, desktop, device, dishwasher, dryer, floorlamp, generic, heater, heatexchange, heatpump, kettle, laundry, laundry2, machine, meter, microwave, pump, smartconsumer, tool, waterheater] (optional)
      phases: # Maximum number of phases, Used to determine the minimum solar surplus and plan duration. [1, 2, 3] (optional)
      minCurrent: 6 # Minimum current (A), The minimum current per connected phase that can be used (optional)
      maxCurrent: 16 # Maximum current (A), The maximum current per connected phase that can be used (optional)
      maxPower: 10000 # Maximum charging power hint (W), Defines the maximum charging power of the vehicle. Helps improve charge plan accuracy when the vehicle typically uses less than the offered current or supports higher single-phase current compared to three-phase. _The offered current of the loadpoint is not affected._ (optional)
      identifiers: # RFID identification, Used to assign the vehicle to a loadpoint via RFID card. The identifier is shown on the loadpoint. Copy the value from there. [Learn more.](https://docs.evcc.io/en/docs/features/vehicle) (optional)
      priority: # Priority, Priority of the loadpoint or vehicle in relation to other loadpoints or vehicles for allocating surplus energy (higher values, higher priority). (optional)
      climaterdisabled: # Disable climater detection, Ignore the vehicle's climater state for charge control, so charging stops at the SoC or energy limit even when the vehicle signals an active climater request. (optional)
      autodetectdisabled: # Exclude from auto detection, Ignore the vehicle for auto detection of connected vehicles (optional)
      wakeupdisabled: # Disable wake-up calls, Do not send wake-up calls to the vehicle or charger, useful for vehicles that refuse to charge after repeated wake-ups. (optional)
      sandbox: # Sandbox environment, optional
      cache: 15m # Cache, Time interval for data refresh (optional)
params:
  - name: apikey
    example:
    default:
    choice: []
    unit:
    description: API Key
    help: Request an API key at [go.skoda.eu/api-keys](https://go.skoda.eu/api-keys).
    advanced: false
    optional: false
  - name: vin
    example: W...
    default:
    choice: []
    unit:
    description: Vehicle Identification Number
    help: If you own multiple vehicles from the same manufacturer
    advanced: false
    optional: false
  - name: title
    example:
    default:
    choice: []
    unit:
    description: Title
    help: Will be displayed in the user interface
    advanced: false
    optional: true
  - name: capacity
    example: 50
    default:
    choice: []
    unit: kWh
    description: Battery capacity
    help:
    advanced: false
    optional: true
  - name: timeout
    example: 10s
    default:
    choice: []
    unit:
    description: Timeout
    help:
    advanced: false
    optional: true
  - name: icon
    example:
    default: car
    choice: ['car', 'bike', 'bus', 'moped', 'motorcycle', 'rocket', 'scooter', 'taxi', 'tractor', 'rickshaw', 'shuttle', 'van', 'airpurifier', 'battery', 'bulb', 'climate', 'coffeemaker', 'compute', 'cooking', 'cooler', 'desktop', 'device', 'dishwasher', 'dryer', 'floorlamp', 'generic', 'heater', 'heatexchange', 'heatpump', 'kettle', 'laundry', 'laundry2', 'machine', 'meter', 'microwave', 'pump', 'smartconsumer', 'tool', 'waterheater']
    unit:
    description: Icon
    help: Will be displayed in the user interface
    advanced: true
    optional: true
  - name: phases
    example:
    default:
    choice: ['1', '2', '3']
    unit:
    description: Maximum number of phases
    help: Used to determine the minimum solar surplus and plan duration.
    advanced: true
    optional: true
  - name: minCurrent
    example: 6
    default:
    choice: []
    unit: A
    description: Minimum current
    help: The minimum current per connected phase that can be used
    advanced: true
    optional: true
  - name: maxCurrent
    example: 16
    default:
    choice: []
    unit: A
    description: Maximum current
    help: The maximum current per connected phase that can be used
    advanced: true
    optional: true
  - name: maxPower
    example: 10000
    default:
    choice: []
    unit: W
    description: Maximum charging power hint
    help: Defines the maximum charging power of the vehicle. Helps improve charge plan accuracy when the vehicle typically uses less than the offered current or supports higher single-phase current compared to three-phase. _The offered current of the loadpoint is not affected._
    advanced: true
    optional: true
  - name: identifiers
    example:
    default:
    choice: []
    unit:
    description: RFID identification
    help: Used to assign the vehicle to a loadpoint via RFID card. The identifier is shown on the loadpoint. Copy the value from there. [Learn more.](https://docs.evcc.io/en/docs/features/vehicle)
    advanced: true
    optional: true
  - name: priority
    example:
    default:
    choice: []
    unit:
    description: Priority
    help: Priority of the loadpoint or vehicle in relation to other loadpoints or vehicles for allocating surplus energy (higher values, higher priority).
    advanced: true
    optional: true
  - name: climaterdisabled
    example:
    default:
    choice: []
    unit:
    description: Disable climater detection
    help: Ignore the vehicle's climater state for charge control, so charging stops at the SoC or energy limit even when the vehicle signals an active climater request.
    advanced: true
    optional: true
  - name: autodetectdisabled
    example:
    default:
    choice: []
    unit:
    description: Exclude from auto detection
    help: Ignore the vehicle for auto detection of connected vehicles
    advanced: true
    optional: true
  - name: wakeupdisabled
    example:
    default:
    choice: []
    unit:
    description: Disable wake-up calls
    help: Do not send wake-up calls to the vehicle or charger, useful for vehicles that refuse to charge after repeated wake-ups.
    advanced: true
    optional: true
  - name: sandbox
    example:
    default:
    choice: []
    unit:
    description: Sandbox environment
    help:
    advanced: true
    optional: true
  - name: cache
    example: 5m
    default: 15m
    choice: []
    unit:
    description: Cache
    help: Time interval for data refresh
    advanced: true
    optional: true