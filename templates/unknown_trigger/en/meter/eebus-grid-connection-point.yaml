template: eebus-mgcp
product:
  identifier: eebus-grid-connection-point
  description: EEBus: grid connection point
  group: Generic support
requirements: ["eebus"]
description: |
  EEBus metering device at the grid connection point using use case MGCP (Monitoring of Grid Connection Point).
render:
  - usage: grid
    default: |
      type: template
      template: eebus-mgcp
      usage: grid
      ski: # SKI (Subject Key Identifier), Usually found on the web interface of the wallbox
      host: 192.0.2.2 # IP address or hostname, If not specified, the device will be discovered automatically via mDNS.
params:
  - name: ski
    example:
    default:
    choice: []
    unit:
    description: SKI (Subject Key Identifier)
    help: Usually found on the web interface of the wallbox
    advanced: false
    optional: false
  - name: host
    example: 192.0.2.2
    default:
    choice: []
    unit:
    description: IP address or hostname
    help: If not specified, the device will be discovered automatically via mDNS.
    advanced: false
    optional: false