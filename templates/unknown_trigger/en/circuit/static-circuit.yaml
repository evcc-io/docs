template: static
product:
  identifier: static-circuit
  description: Static circuit
render:
  - default: |
      type: template
      template: static
      title: Main circuit # Title, Will be displayed in the user interface
      maxcurrent: 16 # Maximum current (A), Maximum current per phase allowed by the load management system. (optional)
      maxpower: 10000 # Maximum power (W), Maximum power allowed by the load management system. The system dynamically distributes available power across all connected chargers and circuits. (optional)
      meter: # Meter ID, optional
      parent: # Parent circuit, optional
params:
  - name: title
    example: Main circuit
    default:
    choice: []
    unit:
    description: Title
    help: Will be displayed in the user interface
    advanced: false
    optional: false
  - name: maxcurrent
    example: 16
    default:
    choice: []
    unit: A
    description: Maximum current
    help: Maximum current per phase allowed by the load management system.
    advanced: false
    optional: true
  - name: maxpower
    example: 10000
    default:
    choice: []
    unit: W
    description: Maximum power
    help: Maximum power allowed by the load management system. The system dynamically distributes available power across all connected chargers and circuits.
    advanced: false
    optional: true
  - name: meter
    example:
    default:
    choice: []
    unit:
    description: Meter ID
    help:
    advanced: false
    optional: true
  - name: parent
    example:
    default:
    choice: []
    unit:
    description: Parent circuit
    help:
    advanced: false
    optional: true