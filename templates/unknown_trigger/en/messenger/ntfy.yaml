template: ntfy
product:
  identifier: ntfy
  brand: Ntfy
render:
  - default: |
      type: template
      template: ntfy
      host: ntfy.sh # IP address or hostname
      topics: # Topics, One entry per line.
      priority: default # Priority, Messages have a priority level that determines how urgently your phone notifies you. On Android, you can customize notification sounds and vibration patterns based on these priority levels. For more details, see [docs.ntfy.sh](https://docs.ntfy.sh/publish#message-priority). [max, high, default, low, min]
      authtoken: tk_7eevizlsiwf9yi4uxsrs83r4352o0 # Access token, Used for secure access to the private ntfy server. (optional)
      tags: # Tags & emojis, Messages can be tagged with emojis or strings, which will be shown in the title or message. For more details, see [docs.ntfy.sh](https://docs.ntfy.sh/publish#tags-emojis). One entry per line. (optional)
params:
  - name: host
    example: ntfy.sh
    default: ntfy.sh
    choice: []
    unit:
    description: IP address or hostname
    help:
    advanced: false
    optional: false
  - name: topics
    example: evcc_alert
    default:
    choice: []
    unit:
    description: Topics
    help: One entry per line.
    advanced: false
    optional: false
  - name: priority
    example:
    default: default
    choice: ['max', 'high', 'default', 'low', 'min']
    unit:
    description: Priority
    help: Messages have a priority level that determines how urgently your phone notifies you. On Android, you can customize notification sounds and vibration patterns based on these priority levels. For more details, see [docs.ntfy.sh](https://docs.ntfy.sh/publish#message-priority).
    advanced: false
    optional: false
  - name: authtoken
    example: tk_7eevizlsiwf9yi4uxsrs83r4352o0
    default:
    choice: []
    unit:
    description: Access token
    help: Used for secure access to the private ntfy server.
    advanced: false
    optional: true
  - name: tags
    example: electric_plug
    default:
    choice: []
    unit:
    description: Tags & emojis
    help: Messages can be tagged with emojis or strings, which will be shown in the title or message. For more details, see [docs.ntfy.sh](https://docs.ntfy.sh/publish#tags-emojis). One entry per line.
    advanced: false
    optional: true