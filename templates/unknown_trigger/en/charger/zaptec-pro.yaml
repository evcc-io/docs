template: zaptec
product:
  identifier: zaptec-pro
  brand: Zaptec
  description: Pro
capabilities: ["rfid"]
requirements: ["sponsorship"]
render:
  - default: |
      type: template
      template: zaptec
      id: # ID, Charger ID (optional)
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
    advanced: |
      type: template
      template: zaptec
      id: # ID, Charger ID (optional)
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
      passive: # Passive mode, evcc only grants charging authorisations/blocks (no current control). Only activate this option if the wallbox is part of an external load management system and rejects current changes (HTTP 403). (optional)
params:
  - name: id
    example:
    default:
    choice: []
    unit:
    description: ID
    help: Charger ID
    advanced: false
    optional: true
  - name: user
    example:
    default:
    choice: []
    unit:
    description: Username
    help: e.g. email address, user id, etc.
    advanced: false
    optional: true
  - name: password
    example:
    default:
    choice: []
    unit:
    description: Password
    help: Use single quotes in case of leading zeros
    advanced: false
    optional: true
  - name: passive
    example:
    default:
    choice: []
    unit:
    description: Passive mode
    help: evcc only grants charging authorisations/blocks (no current control). Only activate this option if the wallbox is part of an external load management system and rejects current changes (HTTP 403).
    advanced: true
    optional: true