template: mystrom
product:
  identifier: mystrom-switch
  brand: myStrom
  description: Switch
  group: Schaltbare Steckdosen
render:
  - usage: pv
    default: |
      type: template
      template: mystrom
      usage: pv
      host: 192.0.2.2 # IP-Adresse oder Hostname
      token: # Token, API-Token, nur erforderlich wenn ein Token auf Gerät dem gesetzt ist (Experte -> REST API aktivieren -> Token) (optional)
  - usage: charge
    default: |
      type: template
      template: mystrom
      usage: charge
      host: 192.0.2.2 # IP-Adresse oder Hostname
      token: # Token, API-Token, nur erforderlich wenn ein Token auf Gerät dem gesetzt ist (Experte -> REST API aktivieren -> Token) (optional)
params:
  - name: host
    example: 192.0.2.2
    default:
    choice: []
    unit:
    description: IP-Adresse oder Hostname
    help:
    advanced: false
    optional: false
  - name: token
    example:
    default:
    choice: []
    unit:
    description: Token
    help: API-Token, nur erforderlich wenn ein Token auf Gerät dem gesetzt ist (Experte -> REST API aktivieren -> Token)
    advanced: false
    optional: true