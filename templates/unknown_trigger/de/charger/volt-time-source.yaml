template: volttime
product:
  identifier: volt-time-source
  brand: Volt Time
  description: Source
capabilities: ["meter", "dim"]
requirements: ["sponsorship"]
render:
  - default: |
      type: template
      template: volttime
      token: # Token, API Token ([developer.volttime.com](https://developer.volttime.com/api-reference/authentication#personal-access-tokens))
      serial: # Seriennummer
params:
  - name: token
    example:
    default:
    choice: []
    unit:
    description: Token
    help: API Token ([developer.volttime.com](https://developer.volttime.com/api-reference/authentication#personal-access-tokens))
    advanced: false
    optional: false
  - name: serial
    example:
    default:
    choice: []
    unit:
    description: Seriennummer
    help:
    advanced: false
    optional: false