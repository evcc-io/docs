type: tronity
name: Tronity Cloud Service
sample: |
  title: Golf # display name for UI
  capacity: 10 # kWh
  credentials:
    id: # user id
    secret: # secret
  tokens:
    access: # access token
    refresh: # refresh token
  vin: W... # VIN
  cache: 5m # optional
