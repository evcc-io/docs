type: tronity
name: Tronity Cloud Service
sample: |
  title: Golf # display name for UI
  capacity: 10 # kWh
  credentials:
    id: # user id
    secret: # secret
  vin: W... # VIN optional
  cache: 5m # optional
