---
title: Vaillant SensoNET (SG Ready)
custom_edit_url: https://github.com/evcc-io/evcc/blob/master/templates/definition/charger/vaillant.yaml
hide_table_of_contents: true
---

import DeviceConfig from '@site/src/components/DeviceConfig';
import DeviceFeatures from '@site/src/components/DeviceFeatures';
import SponsorshipRequired from '/docs/_sponsorship_required.mdx';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import '@evcc/icons';

# Vaillant SensoNET (SG Ready)

<div className="device-detail-container">
  <div className="device-detail-content">
    <DeviceFeatures features="" />

The boost function heats hot water or a boost zone. The boost zone is identified by the ID. The boost temperature is specified in degrees Celsius. If boost temperature is specified, the boost zone is activated, otherwise hot water.


<DeviceConfig code={`chargers:
    - name: my_charger
      type: template
      template: vaillant
      user: # Username, e.g. email address, user id, etc. (optional)
      password: # Password, Use single quotes in case of leading zeros (optional)
      realm: DE # Region, [DE, AT] (optional)
      zone: # Boost zone ID, optional
      setpoint: # Boost temperature, optional`}  />
  </div>
  <div className="device-detail-icon">
    <evcc-icon
      type="charger"
      name="vaillant-sensonet-sg-ready"
      size="400px"
      alt="Vaillant SensoNET (SG Ready)"
      className="device-detail-icon-img"
    ></evcc-icon>
  </div>
</div>

## Template Parameter `vaillant`

| Name | Explanation | Value | Optional |
|-----------|-------------|-------|----------|
| Username<br/>`user` | e.g. email address, user id, etc. |  | no |
| Password<br/>`password` | Use single quotes in case of leading zeros |  | no |
| Region<br/>`realm` |  | Standard: `DE`<br/>Auswahl: `DE`, `AT` | no |
| Boost zone ID<br/>`zone` |  |  | no |
| Boost temperature<br/>`setpoint` |  |  | no |

