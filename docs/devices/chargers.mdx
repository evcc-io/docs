---
sidebar_position: 1
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Config from "/src/components/ConfigCode";
import SponsorshipRequired from "/docs/_sponsorship_required.mdx";

# Wallboxen

Zur Steuerung der Ladung muss evcc mit einer Wallbox kommunizieren.

In der Konfigurationsdatei gibt es hierzu den folgenden Bereich:

```yaml
charger:
  - name: wallbox
    type: ...
```

Auf dieser Seite findest du die Konfigurationen für alle derzeit von evcc direkt unterstützten Wallboxen.
Nachdem du das Code-Beispiel für deine Wallbox angepasst und in die `evcc.yaml` übernommen hast, kannst du die Verbindung mit dem Befehl `evcc charger` testen.

```
$ ./evcc charger

Power:          0W
Current L1..L3: 0A 0A 0A
Charge status:  A
Enabled:        false
```

{/* AUTO-GENERATED CONTENT BELOW THIS LINE */}


## EVSE DIN

```yaml
chargers:
  - name: my_charger
    type: template
    template: evse_din
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 9600 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8N1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

## EVSE-Wifi

```yaml
chargers:
  - name: my_charger
    type: template
    template: evsewifi
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## openWB (MQTT)
Die Wallbox muss als Ladepunkt konfiguriert sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: openwb
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    loadpointid: 1  # Optional 
```

## ABL eMH

```yaml
chargers:
  - name: my_charger
    type: template
    template: abl
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 38400 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8E1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

## Alfen Eve

```yaml
chargers:
  - name: my_charger
    type: template
    template: alfen
    # Modbus Start
    id: 1
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

<SponsorshipRequired />

## Alphatec Wallbox Mini

```yaml
chargers:
  - name: my_charger
    type: template
    template: alphatec
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 9600 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8N1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

## BMW i Wallbox
Es muss eine sogenannte UDP Funktion über den DIP Schalter 1.3 eingeschaltet (ON) werden. Die Installationsanleitung der Wallbox hilft hier weiter.

```yaml
chargers:
  - name: my_charger
    type: template
    template: keba
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## DaheimLaden Wallbox (Cloud API)

```yaml
chargers:
  - name: my_charger
    type: template
    template: daheimladen
    token: # Sie bekommen dieses Access Token vom Daheimladen Support: info@daheimladen.de  # Optional 
    stationid: xxxxxxxxxxxx990  # Die Software Seriennummer, welche auf dem Bildschirm der Wallbox angezeigt wird  # Optional 
```

## Easee Home (Cloud API)

```yaml
chargers:
  - name: my_charger
    type: template
    template: easee
    user: # Benutzername um auf das Gerät zuzugreifen 
    password: # Passwort des Benutzerkontos 
    charger: EH______ 
```

<SponsorshipRequired />

## Heidelberg Energy Control (Modbus RTU)

```yaml
chargers:
  - name: my_charger
    type: template
    template: heidelberg
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 19200 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8E1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

## Innogy eBox

```yaml
chargers:
  - name: my_charger
    type: template
    template: innogy-ebox
    # Modbus Start
    id: 1
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

<SponsorshipRequired />

## KEBA
### KeContact P20
Es muss eine sogenannte UDP Funktion über den DIP Schalter 1.3 eingeschaltet (ON) werden. Die Installationsanleitung der Wallbox hilft hier weiter.

```yaml
chargers:
  - name: my_charger
    type: template
    template: keba
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

### KeContact P30
Es muss eine sogenannte UDP Funktion über den DIP Schalter 1.3 eingeschaltet (ON) werden. Die Installationsanleitung der Wallbox hilft hier weiter.

```yaml
chargers:
  - name: my_charger
    type: template
    template: keba
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## Mennekes
### AMTRON PREMIUM

```yaml
chargers:
  - name: my_charger
    type: template
    template: amtron
    # Modbus Start
    id: 255
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

<SponsorshipRequired />

### AMTRON XTRA

```yaml
chargers:
  - name: my_charger
    type: template
    template: amtron
    # Modbus Start
    id: 255
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

<SponsorshipRequired />

## NRGKick
### Bluetooth

```yaml
chargers:
  - name: my_charger
    type: template
    template: nrgkick-bluetooth
    mac:
    pin: 
```

### Connect

```yaml
chargers:
  - name: my_charger
    type: template
    template: nrgkick-connect
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    mac:
    password: # Passwort des Benutzerkontos 
```

## PC Electric Garo
Es können momentan nur als Master konfigurierte Geräte verwendet werden!

```yaml
chargers:
  - name: my_charger
    type: template
    template: pcelectric-garo
    host: 192.0.2.2  # IP-Adresse oder Hostname  # Optional 
    port: 8080  # Port  # Optional 
```

<SponsorshipRequired />

## Porsche
### Mobile Charger Connect

```yaml
chargers:
  - name: my_charger
    type: template
    template: pmcc
    ski: # Die SKI der Wallbox, ist üblicherweise im Web Interface der Wallbox zu finden 
    enforcePVLimits: true  # Verwende PV Laden über eine Ladebegrenzung (wie herkömmliche Wallboxen) bei Fahrzeugen mit Unterstützung von ISO15118  # Optional 
```

### Mobile Charger Plus

```yaml
chargers:
  - name: my_charger
    type: template
    template: pmcp
    ski: # Die SKI der Wallbox, ist üblicherweise im Web Interface der Wallbox zu finden 
```

## SENEC
### Wallbox pro

```yaml
chargers:
  - name: my_charger
    type: template
    template: abl
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 38400 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8E1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

### Wallbox pro s

```yaml
chargers:
  - name: my_charger
    type: template
    template: heidelberg
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 19200 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8E1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

## TinkerForge
### WARP Charger
Es muss ein sogennanter MQTT Broker auf einem Computer installiert sein. Die MQTT Verbindung muss in der Wallbox eingerichtet sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: tinkerforge-warp
    host: 192.0.2.2  # Die IP Adresse oder der Hostname des MQTT Brokers 
    port: 1883  # Der Port des MQTT Brokers  # Optional 
    topic: warp  # Optional 
    timeout: 30s  # Akzeptiere keine Daten die älter als dieser Wert sind  # Optional 
```

### WARP Charger Pro
Es muss ein sogennanter MQTT Broker auf einem Computer installiert sein. Die MQTT Verbindung muss in der Wallbox eingerichtet sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: tinkerforge-warp-pro
    host: 192.0.2.2  # Die IP Adresse oder der Hostname des MQTT Brokers 
    port: 1883  # Der Port des MQTT Brokers  # Optional 
    topic: warp  # Optional 
    timeout: 30s  # Akzeptiere keine Daten die älter als dieser Wert sind  # Optional 
```

## Vestel EVC04

```yaml
chargers:
  - name: my_charger
    type: template
    template: vestel
    # Modbus Start
    id: 255
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

<SponsorshipRequired />

## Wallbe
### Eco
Die Wallbox muss über ein Netzwerkkabel angebunden sein und im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Eco (mit Strommessgerät)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-meter
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Eco (vor ~2019)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-pre2019
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Eco (vor ~2019, mit Strommessgerät)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-pre2019-meter
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Pro
Die Wallbox muss über ein Netzwerkkabel angebunden sein und im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Pro (mit Strommessgerät)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-meter
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Pro (vor ~2019)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-pre2019
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Pro (vor ~2019, mit Strommessgerät)
Im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: wallbe-pre2019-meter
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

## Walther Werke Basic Evo Pro

```yaml
chargers:
  - name: my_charger
    type: template
    template: heidelberg
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 19200 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8E1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

<SponsorshipRequired />

## Webasto Live

```yaml
chargers:
  - name: my_charger
    type: template
    template: webasto
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

## cFos PowerBrain

```yaml
chargers:
  - name: my_charger
    type: template
    template: cfos
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

<SponsorshipRequired />

## go-e
### Charger HOME+
Benötigt mindestens Firmware 040.0 oder neuer.

```yaml
chargers:
  - name: my_charger
    type: template
    template: go-e
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

### Charger HOME+ (V3)
Benötigt mindestens Firmware 052.1 oder neuer. 

Für 1P/3P-Phasenumschaltung muss die HTTP API v2 im Charger aktiviert sein.      

```yaml
chargers:
  - name: my_charger
    type: template
    template: go-e-v3
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

### Charger HOMEfix
Benötigt mindestens Firmware 040.0 oder neuer.

```yaml
chargers:
  - name: my_charger
    type: template
    template: go-e
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

### Charger HOMEfix (V3)
Benötigt mindestens Firmware 052.1 oder neuer. 

Für 1P/3P-Phasenumschaltung muss die HTTP API v2 im Charger aktiviert sein.      

```yaml
chargers:
  - name: my_charger
    type: template
    template: go-e-v3
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

### Charger Pro
Benötigt mindestens Firmware 040.0 oder neuer.

```yaml
chargers:
  - name: my_charger
    type: template
    template: go-e
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## hardybarth-ecb1 HardyBarth cPH1 (eCB1 controller)

```yaml
chargers:
  - name: my_charger
    type: template
    template: hardybarth-ecb1
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## hardybarth-salia HardyBarth (Salia PLCC controller)

```yaml
chargers:
  - name: my_charger
    type: template
    template: hardybarth-salia
    host: 192.0.2.2  # IP-Adresse oder Hostname 
```

## i-CHARGE CION

```yaml
chargers:
  - name: my_charger
    type: template
    template: ichargecion
    # Modbus Start
    id: 1
    # TCPIP
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    # Modbus End 
```

## Generische Unterstützung
### 
### EEBUS kompatible Wallbox

```yaml
chargers:
  - name: my_charger
    type: template
    template: eebus
    ski: # Die SKI der Wallbox, ist üblicherweise im Web Interface der Wallbox zu finden 
    enforcePVLimits: true  # Verwende PV Laden über eine Ladebegrenzung (wie herkömmliche Wallboxen) bei Fahrzeugen mit Unterstützung von ISO15118  # Optional 
```

### Phoenix EM-CP-PP-ETH Controller (Modbus TCP)
Die Wallbox muss über ein Netzwerkkabel angebunden sein und im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: phoenix-em-eth
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Phoenix EV-ETH Controller (Modbus TCP)
Die Wallbox muss über ein Netzwerkkabel angebunden sein und im Gerät muss der DIP Schalter 10 auf 'ON' gestellt sein.

```yaml
chargers:
  - name: my_charger
    type: template
    template: phoenix-ev-eth
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    port: 502  # Port  # Optional 
```

### Phoenix EV-SER Controller (Modbus RTU)

```yaml
chargers:
  - name: my_charger
    type: template
    template: phoenix-ev-ser
    # Modbus Start
    id: 1
    # RS485 via adapter:
    device: /dev/ttyUSB0 # USB-RS485 Adapter Adresse
    baudrate: 9600 # Prüfe die Geräteeinstellungen, typische Werte sind 9600, 19200, 38400, 57600, 115200
    comset: "8N1" # Kommunikationsparameter für den Adapter
    # RS485 via TCPIP:
    uri: 192.0.2.2:502 # IP-Adresse oder Hostname: Port
    rtu: true
    # Modbus End 
```

## Schaltbare Steckdosen
### Shelly

```yaml
chargers:
  - name: my_charger
    type: template
    template: shelly
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    standbypower: 15  # Leistung oberhalb des angegebenen Wertes wird als Ladeleistung gewertet  # Optional 
```

### Tasmota

```yaml
chargers:
  - name: my_charger
    type: template
    template: tasmota
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    user: # (optional) nur erforderlich falls diese Werte im Gerät gesetzt sind  # Optional 
    password: # (optional) nur erforderlich falls diese Werte im Gerät gesetzt sind  # Optional 
    standbypower: 15  # Leistung oberhalb des angegebenen Wertes wird als Ladeleistung gewertet  # Optional 
```

### AVM FritzDECT

```yaml
chargers:
  - name: my_charger
    type: template
    template: fritzdect
    uri: https://fritz.box  # Optional 
    user: # Benutzername um auf das Gerät zuzugreifen 
    password: # Passwort des Benutzerkontos 
    ain: 007788992233  # Die AIN ist auf dem Typenschild auf der Geräterückseite aufgedruckt. 
    standbypower: 15  # Leistung oberhalb des angegebenen Wertes wird als Ladeleistung gewertet  # Optional 
```

### TP-LINK Smart Plug

```yaml
chargers:
  - name: my_charger
    type: template
    template: tplink
    host: 192.0.2.2  # IP-Adresse oder Hostname 
    standbypower: 15  # Leistung oberhalb des angegebenen Wertes wird als Ladeleistung gewertet  # Optional 
```
