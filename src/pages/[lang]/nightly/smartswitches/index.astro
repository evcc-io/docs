---
import SmartswitchesList from "@components/lists/SmartswitchesList.astro";

export async function getStaticPaths() {
  return [{ params: { lang: "en" } }, { params: { lang: "de" } }];
}

const lang = Astro.params.lang as "de" | "en";
---

<SmartswitchesList lang={lang} channel="nightly" />
