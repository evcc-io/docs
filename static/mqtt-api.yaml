asyncapi: 3.0.0
info:
  title: evcc MQTT-API
  version: latest
servers:
  evcc:
    host: "{IP}:{PORT}"
    protocol: mqtt
    description: Your setup mqtt server.
    variables:
      IP:
        default: 192.168.XXX.XXX
      PORT:
