asyncapi: 3.0.0
info:
  title: evcc MQTT-API
  version: latest
operations:
  GeneralStatusReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/status"
      messages:
        status:
          description: TODO
          payload:
            type: string
            enum:
              - online
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/general"
  GeneralUpdatedReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/updated"
      messages:
        updated:
          description: TODO
          payload:
            type: integer
            example: 1740512847
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/general"
  LoadpointBatteryBoostReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/batteryBoost"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        batteryBoost:
          $ref: "#/components/messages/batteryBoost"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointBatteryBoostSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/batteryBoost/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        batteryBoost:
          $ref: "#/components/messages/batteryBoost"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeCurrentReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeCurrent"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeCurrentsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeCurrents"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeCurrents:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargePowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargePower"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargePower:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeRemainingDurationReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeRemainingDuration"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeRemainingDuration:
          description: TODO
          payload:
            type: integer
  LoadpointChargeRemainingEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeRemainingEnergy"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeRemainingEnergy:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeTotalImportReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeTotalImport"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeTotalImport:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeVehicleNameReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleName"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleName:
          $ref: "#/components/messages/vehicleName"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargeVehicleNameSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/vehicle/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleName:
          description: TODO
          payload:
            type: string
            examples:
              - Cupra Born
              - "-"
              - nil
              - "null"
              - none
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargedDurationReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargeDuration"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargeDuration:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargedEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargedEnergy"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargedEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargerFeatureHeatingReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargerFeatureHeating"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargerFeatureHeating:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargerFeatureIntegratedDeviceReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargerFeatureIntegratedDevice"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargerFeatureIntegratedDevice:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargerPhases1p3pReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargerPhases1p3p"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargerPhases1p3p:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargerSinglePhaseReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargerSinglePhase"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargerSinglePhase:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargerStatusReasonReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/chargerStatusReason"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        chargerStatusReason:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointChargingReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/charging"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        charging:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointConnectedDurationReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/connectedDuration"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        connectedDuration:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointConnectedReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/connected"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        connected:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointDisableDelayReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/disableDelay"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        disableDelay:
          $ref: "#/components/messages/disableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointDisableDelaySet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/disableDelay/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        disableDelay:
          $ref: "#/components/messages/disableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointDisableThresholdReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/disableThreshold"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        disableThreshold:
          $ref: "#/components/messages/disableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointDisableThresholdSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/disableThreshold/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        disableThreshold:
          $ref: "#/components/messages/disableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectiveLimitSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectiveLimitSoc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectiveLimitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectiveMaxCurrentReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectiveMaxCurrent"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectiveMaxCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectiveMinCurrentReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectiveMinCurrent"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectiveMinCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectivePlanIdReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectivePlanId"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectivePlanId:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectivePlanSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectivePlanSoc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectivePlanSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEffectivePriorityReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/effectivePriority"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        effectivePriority:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEnableDelayReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/enableDelay"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        enableDelay:
          $ref: "#/components/messages/enableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEnableDelaySet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/enableDelay/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        enableDelay:
          $ref: "#/components/messages/enableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEnableThresholdReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/enableThreshold"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        enableThreshold:
          $ref: "#/components/messages/enableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEnableThresholdSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/enableThreshold/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        enableThreshold:
          $ref: "#/components/messages/enableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointEnabledReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/enabled"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        enabled:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointLimitEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/limitEnergy"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        limitEnergy:
          $ref: "#/components/messages/limitEnergy"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointLimitEnergySet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/limitEnergy/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        limitEnergy:
          $ref: "#/components/messages/limitEnergy"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointLimitSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/limitSoc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        limitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointMaxCurrentReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/maxCurrent"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        maxCurrent:
          $ref: "#/components/messages/maxCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointMaxCurrentSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/maxCurrent/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        maxCurrent:
          $ref: "#/components/messages/maxCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointMinCurrentReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/minCurrent"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        minCurrent:
          $ref: "#/components/messages/minCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointMinCurrentSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/minCurrent/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        minCurrent:
          $ref: "#/components/messages/minCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointModeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/mode"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        mode:
          $ref: "#/components/messages/mode"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointModeSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/mode/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        mode:
          $ref: "#/components/messages/mode"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPhaseActionReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/phaseAction"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        phaseAction:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPhaseRemainingReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/phaseRemaining"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        phaseRemaining:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPhasesActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/phasesActive"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        phasesActive:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPhasesConfiguredReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/phasesConfigured"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        phases:
          $ref: "#/components/messages/phases"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPhasesSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/phases/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        phases:
          $ref: "#/components/messages/phases"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPlanActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/planActive"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        planActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPlanEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/planEnergy"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        planEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPlanEnergySet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/planEnergy/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        planEnergy:
          contentType: application/json
          payload:
            type: object
            properties:
              time:
                description: TODO
                type: string
              value:
                description: TODO
                type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPlanOverrunReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/planOverrun"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        planOverrun:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPriorityReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/priority"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        priority:
          $ref: "#/components/messages/priority"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPrioritySet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/priority/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        priority:
          $ref: "#/components/messages/priority"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPvActionReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/pvAction"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        pvAction:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointPvRemainingReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/pvRemaining"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        pvRemaining:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSessionCo2PerKWhReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/sessionCo2PerKWh"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        sessionCo2PerKWh:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSessionEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/sessionEnergy"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        sessionEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSessionPricePerKWhReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/sessionPricePerKWh"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        sessionPricePerKWh:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSessionPriceReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/sessionPrice"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        sessionPrice:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSessionSolarPercentageReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/sessionSolarPercentage"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        sessionSolarPercentage:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSmartCostActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/smartCostActive"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        smartCostActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointSmartCostLimitSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/smartCostLimit/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        smartCostLimit:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointTitleReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/title"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        loadpointTitle:
          description: >-
            [See
            documentation](/en/docs/reference/configuration/loadpoints#title)
          payload:
            type: string
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleDetectionActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleDetectionActive"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleDetectionActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleLimitSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleLimitSoc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleLimitSoc:
          $ref: "#/components/messages/vehicleLimitSoc"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleLimitSocSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/loadpoints/{id}/vehicleLimitSoc/set"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleLimitSoc:
          $ref: "#/components/messages/vehicleLimitSoc"
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleOdometerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleOdometer"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleOdometer:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleRangeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleRange"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleRange:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleSoc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleSoc:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointVehicleWelcomeActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints/{id}/vehicleWelcomeActive"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        vehicleWelcomeActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  LoadpointsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/loadpoints"
      messages:
        loadpoints:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/loadpoints"
  SiteAuxReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/aux"
      messages:
        aux:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteAvailableVersionReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/availableVersion"
      messages:
        availableVersion:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteriesCapacityReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryCapacity"
      messages:
        batteryCapacity:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteriesPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryPower"
      messages:
        batteryPower:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteriesReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/battery"
      messages:
        batteries:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteriesSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batterySoc"
      messages:
        batterySoc:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryCapacityReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/battery/{id}/capacity"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        capacity:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryControllableReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/battery/{id}/controllable"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        batteryControllable:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryDischargeControlReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryDischargeControl"
      messages:
        batteryDischargeControl:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryEnergy"
      messages:
        batteryEnergy:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryGridChargeActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryGridChargeActive"
      messages:
        batteryGridChargeActive:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryModeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/batteryMode"
      messages:
        batteryMode:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBatteryPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/battery/{id}/power"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        batteryPower:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteBatterySocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/battery/{id}/soc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        batterySoc:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteBufferSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/bufferSoc"
      messages:
        bufferSoc:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteBufferStartSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/bufferStartSoc"
      messages:
        bufferStartSoc:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteCurrencyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/currency"
      messages:
        currency:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteEebusReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/eebus"
      messages:
        eebus:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteExtReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/ext"
      messages:
        ext:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteForecastCo2Receive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/forecast/co2"
      messages:
        forecastCo2:
          description: TODO
          payload:
            type: array
            items:
              type: object
              properties:
                start:
                  type: string
                end:
                  type: string
                price:
                  type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteGreenShareHomeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/greenShareHome"
      messages:
        greenShareHome:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteGreenShareLoadpointsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/greenShareLoadpoints"
      messages:
        greenShareLoadpoints:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteGridConfiguredReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/gridConfigured"
      messages:
        gridConfigured:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteGridEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/grid/energy"
      messages:
        gridEnergy:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteGridPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/grid/power"
      messages:
        gridPower:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteHomePowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/homePower"
      messages:
        homePower:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteInfluxInsecureReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/influx/insecure"
      messages:
        influxInsecure:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteIntervalReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/interval"
      messages:
        interval:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteMessagingReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/messaging"
      messages:
        messaging:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteModbusproxyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/modbusproxy"
      messages:
        modbusproxy:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteMqttBrokerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/mqtt/broker"
      messages:
        mqttBroker:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteMqttInsecureReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/mqtt/insecure"
      messages:
        mqttInsecure:
          description: TODO
          payload:
            type: boolean
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteMqttTopicReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/mqtt/topic"
      messages:
        mqttTopic:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteMqttUserReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/mqtt/user"
      messages:
        mqttUser:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteNetworkHostReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/network/host"
      messages:
        networkHost:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteNetworkPortReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/network/port"
      messages:
        networkPort:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteNetworkSchemaReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/network/schema"
      messages:
        networkSchema:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SitePrioritySocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/prioritySoc"
      messages:
        prioritySoc:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SitePvEnergyReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/pvEnergy"
      messages:
        pvEnergy:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SitePvPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/pv/{id}/power"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        pvPower:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SitePvsPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/pvPower"
      messages:
        pvPower:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SitePvsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/pv"
      messages:
        pvs:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteResidualPowerReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/residualPower"
      messages:
        residualPower:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteSmartCostTypeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/smartCostType"
      messages:
        smartCostType:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteSponsorExpiresAtReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/sponsor/expiresAt"
      messages:
        sponsorExpiresAt:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteSponsorNameReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/sponsor/name"
      messages:
        sponsorName:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteStatisticsAvgCo2Receive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/statistics/{period}/avgCo2"
      parameters:
        period:
          $ref: "#/components/parameters/period"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        statisticsAvgCo2:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteStatisticsAvgPriceReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/statistics/{period}/avgPrice"
      parameters:
        period:
          $ref: "#/components/parameters/period"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        statisticsAvgPrice:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteStatisticsChargedKWhReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/statistics/{period}/chargedKWh"
      parameters:
        period:
          $ref: "#/components/parameters/period"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        statisticsChargedKWh:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteStatisticsSolarPercentageReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/statistics/{period}/solarPercentage"
      parameters:
        period:
          $ref: "#/components/parameters/period"
        topic:
          $ref: "#/components/parameters/topic"
      messages:
        statisticsSolarPercentage:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffCo2HomeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffCo2Home"
      messages:
        tariffCo2Home:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffCo2LoadpointsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffCo2Loadpoints"
      messages:
        tariffCo2Loadpoints:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffCo2Receive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffCo2"
      messages:
        tariffCo2:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffFeedInReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffFeedIn"
      messages:
        tariffFeedIn:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffGridReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffGrid"
      messages:
        tariffGrid:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffPriceHomeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffPriceHome"
      messages:
        tariffPriceHome:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteTariffPriceLoadpointsReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/tariffPriceLoadpoints"
      messages:
        tariffPriceLoadpoints:
          description: TODO
          payload:
            type: number
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  SiteVersionReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/site/version"
      messages:
        version:
          description: TODO
          payload:
            type: string
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/site"
  VehicleCapacityReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/capacity"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        capacity:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleFeatureReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/features/{id}"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        feature:
          description: TODO
          payload:
            type: string
            enum:
              - offline
              - TODO
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleFeaturesReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/features"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        features:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleIconReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/icon"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        icon:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleLimitSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/limitSoc"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        limitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleLimitSocSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/vehicles/{vehicleName}/limitSoc/set"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        limitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleMinSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/minSoc"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        minSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleMinSocSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/vehicles/{vehicleName}/minSoc/set"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        minSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehiclePlanSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/plan/soc"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        soc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehiclePlanSocSet:
    description: TODO
    action: send
    channel:
      address: "{topic}/vehicles/{vehicleName}/planSoc/set"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        planSoc:
          description: TODO
          payload:
            type: object
            properties:
              time:
                description: TODO
                type: string
              value:
                description: TODO
                type: string
    tags:
      - $ref: "#/components/tags/vehicles"
  VehiclePlanTimeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/plan/time"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        time:
          description: TODO
          payload:
            type: string
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanActiveReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans/{id}/active"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        active:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanSocReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans/{id}/soc"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        soc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanTimeReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans/{id}/time"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        time:
          description: TODO
          payload:
            type: string
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanTzReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans/{id}/tz"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        tz:
          description: TODO
          payload:
            type: string
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanWeekdayReceive:
    description: TODO
    action: receive
    channel:
      address: >-
        {topic}/vehicles/{vehicleName}/repeatingPlans/{planId}/weekdays/{weekdayId}
      parameters:
        planId:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
        weekdayId:
          $ref: "#/components/parameters/id"
      messages:
        weekday:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlanWeekdaysReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans/{id}/weekdays"
      parameters:
        id:
          $ref: "#/components/parameters/id"
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        weekdays:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleRepeatingPlansReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/repeatingPlans"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        repeatingPlans:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/vehicles"
  VehicleTitleReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles/{vehicleName}/title"
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
      messages:
        title:
          $ref: "#/components/messages/vehicleName"
    tags:
      - $ref: "#/components/tags/vehicles"
  VehiclesReceive:
    description: TODO
    action: receive
    channel:
      address: "{topic}/vehicles"
      messages:
        vehicles:
          description: TODO
          payload:
            type: integer
      parameters:
        topic:
          $ref: "#/components/parameters/topic"
    tags:
      - $ref: "#/components/tags/vehicles"
components:
  messages:
    batteryBoost:
      description: TODO
      payload:
        type: boolean
    disableDelay:
      description: TODO
      payload:
        type: integer
    disableThreshold:
      description: TODO
      payload:
        type: number
    enableDelay:
      description: TODO
      payload:
        type: integer
    enableThreshold:
      description: TODO
      payload:
        type: number
    limitEnergy:
      description: TODO
      payload:
        type: integer
    maxCurrent:
      description: TODO
      payload:
        type: integer
    minCurrent:
      description: TODO
      payload:
        type: integer
    mode:
      description: >-
        Charging mode. [Read
        more.](/en/docs/reference/configuration/loadpoints#mode)
      payload:
        type: string
        enum:
          - "off"
          - pv
          - minpv
          - now
    phases:
      description: >-
        Number of phases. (0: auto, 1: 1-phase, 3: 3-phase) [Read
        more.](/en/docs/reference/configuration/loadpoints#phases)
      payload:
        type: integer
        enum:
          - "0"
          - "1"
          - "3"
    priority:
      description: TODO
      payload:
        type: integer
    vehicleLimitSoc:
      description: TODO
      payload:
        type: integer
    vehicleName:
      description: TODO
      payload:
        type: string
  parameters:
    id:
      description: The index. Starts with 1.
      examples:
        - "1"
        - "2"
        - "5"
    period:
      description: TODO
      enum:
        - 30d
        - 365d
        - thisYear
        - total
    topic:
      description: TODO
      examples:
        - evcc
    vehicleName:
      description: TODO
      examples:
        - db:19
        - vehicle_1
  tags:
    general:
      name: General
    loadpoints:
      name: Loadpoints
    site:
      name: Site
    vehicles:
      name: Vehicles
