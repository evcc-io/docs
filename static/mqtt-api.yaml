# TODO:
# - check data types

asyncapi: 3.0.0
info:
  title: evcc MQTT-API
  version: latest
operations:
  receiveLoadpointTitle:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/title
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        loadpointTitle:
          description: "[See documentation](/en/docs/reference/configuration/loadpoints#title)"
          payload:
            type: string
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPriority:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/priority
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        priority:
          $ref: "#/components/messages/priority"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointPriority:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/priority
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        priority:
          $ref: "#/components/messages/priority"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointMinCurrent:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/minCurrent
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        minCurrent:
          $ref: "#/components/messages/minCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointMinCurrent:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/minCurrent/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        minCurrent:
          $ref: "#/components/messages/minCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointMaxCurrent:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/maxCurrent
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        maxCurrent:
          $ref: "#/components/messages/maxCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointMaxCurrent:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/maxCurrent/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        maxCurrent:
          $ref: "#/components/messages/maxCurrent"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEnableThreshold:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/enableThreshold
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        enableThreshold:
          $ref: "#/components/messages/enableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointEnableThreshold:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/enableThreshold/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        enableThreshold:
          $ref: "#/components/messages/enableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointDisableThreshold:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/disableThreshold
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        disableThreshold:
          $ref: "#/components/messages/disableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointDisableThreshold:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/disableThreshold/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        disableThreshold:
          $ref: "#/components/messages/disableThreshold"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEnableDelay:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/enableDelay
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        enableDelay:
          $ref: "#/components/messages/enableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointEnableDelay:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/enableDelay/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        enableDelay:
          $ref: "#/components/messages/enableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointDisableDelay:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/disableDelay
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        disableDelay:
          $ref: "#/components/messages/disableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointDisableDelay:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/disableDelay/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        disableDelay:
          $ref: "#/components/messages/disableDelay"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPhasesConfigured:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/phasesConfigured
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        phases:
          $ref: "#/components/messages/phases"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointPhases:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/phases/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        phases:
          $ref: "#/components/messages/phases"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargerPhases1p3p:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargerPhases1p3p
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargerPhases1p3p:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargerSinglePhase:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargerSinglePhase
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargerSinglePhase:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPhasesActive:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/phasesActive
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        phasesActive:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPhaseAction:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/phaseAction
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        phaseAction:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPhaseRemaining:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/phaseRemaining
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        phaseRemaining:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPvAction:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/pvAction
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        pvAction:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPvRemaining:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/pvRemaining
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        pvRemaining:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargerFeatureIntegratedDevice:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargerFeatureIntegratedDevice
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargerFeatureIntegratedDevice:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargerFeatureHeating:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargerFeatureHeating
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargerFeatureHeating:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleOdometer:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleOdometer
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleOdometer:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleSoc:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleSoc
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleSoc:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleRange:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleRange
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleRange:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleLimitSoc:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleLimitSoc
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleLimitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectivePriority:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectivePriority
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectivePriority:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectivePlanId:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectivePlanId
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectivePlanId:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectivePlanSoc:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectivePlanSoc
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectivePlanSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectiveMinCurrent:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectiveMinCurrent
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectiveMinCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectiveMaxCurrent:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectiveMaxCurrent
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectiveMaxCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEffectiveLimitSoc:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/effectiveLimitSoc
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        effectiveLimitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleDetectionActive:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleDetectionActive
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleDetectionActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPlanEnergy:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/planEnergy
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        planEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointLimitSoc:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/limitSoc
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        limitSoc:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointLimitEnergy:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/limitEnergy
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        limitEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPlanActive:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/planActive
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        planActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointBatteryBoost:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/batteryBoost
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        batteryBoost:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargeCurrent:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeCurrent
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeCurrent:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargePower:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargePower
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargePower:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargeCurrents:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeCurrents
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeCurrents:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSmartCostActive:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/smartCostActive
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        smartCostActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSessionEnergy:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/sessionEnergy
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        sessionEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSessionSolarPercentage:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/sessionSolarPercentage
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        sessionSolarPercentage:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargedEnergy:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargedEnergy
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargedEnergy:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargedDuration:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeDuration
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeDuration:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargeTotalImport:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeTotalImport
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeTotalImport:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointConnectedDuration:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/connectedDuration
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        connectedDuration:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointVehicleWelcomeActive:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleWelcomeActive
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleWelcomeActive:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointConnected:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/connected
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        connected:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointCharging:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/charging
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        charging:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointEnabled:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/enabled
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        enabled:
          description: TODO
          payload:
            type: boolean
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointPlanOverrun:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/planOverrun
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        planOverrun:
          description: TODO
          payload:
            type: integer
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSessionPricePerKWh:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/sessionPricePerKWh
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        sessionPricePerKWh:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSessionPrice:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/sessionPrice
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        sessionPrice:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointSessionCo2PerKWh:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/sessionCo2PerKWh
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        sessionCo2PerKWh:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargerStatusReason:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargerStatusReason
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargerStatusReason:
          description: TODO
          payload:
            type: string
            enum:
              - TODO
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargeRemainingEnergy:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeRemainingEnergy
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeRemainingEnergy:
          description: TODO
          payload:
            type: number
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointChargeRemainingDuration:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/chargeRemainingDuration
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        chargeRemainingDuration:
          description: TODO
          payload:
            type: integer
  receiveLoadpointChargeVehicleName:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/vehicleName
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointChargeVehicleName:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/vehicle/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        vehicleName:
          $ref: "#/components/parameters/vehicleName"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveLoadpointMode:
    description: TODO
    action: receive
    channel:
      address: evcc/loadpoint/{id}/mode
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        mode:
          $ref: "#/components/messages/mode"
    tags:
      - $ref: "#/components/tags/loadpoints"
  sendLoadpointMode:
    description: TODO
    action: send
    channel:
      address: evcc/loadpoint/{id}/mode/set
      parameters:
        id:
          $ref: "#/components/parameters/id"
      messages:
        mode:
          $ref: "#/components/messages/mode"
    tags:
      - $ref: "#/components/tags/loadpoints"
  receiveStatus:
    description: TODO
    action: receive
    channel:
      address: evcc/status
      messages:
        status:
          description: TODO
          payload:
            type: string
            enum:
              - online
    tags:
      - $ref: "#/components/tags/general"
  receiveUpdated:
    description: TODO
    action: receive
    channel:
      address: evcc/updated
      messages:
        updated:
          description: TODO
          payload:
            type: integer
            example: 1740512847
    tags:
      - $ref: "#/components/tags/general"
components:
  messages:
    mode:
      description: Charging mode. [Read more.](/en/docs/reference/configuration/loadpoints#mode)
      payload:
        type: string
        enum:
          - off
          - pv
          - minpv
          - now
    phases:
      description: "Number of phases. (0: auto, 1: 1-phase, 3: 3-phase) [Read more.](/en/docs/reference/configuration/loadpoints#phases)"
      payload:
        type: integer
        enum:
          - "0"
          - "1"
          - "3"
    priority:
      description: TODO
      payload:
        type: integer
    minCurrent:
      description: TODO
      payload:
        type: integer
    maxCurrent:
      description: TODO
      payload:
        type: integer
    enableThreshold:
      description: TODO
      payload:
        type: number
    disableThreshold:
      description: TODO
      payload:
        type: number
    enableDelay:
      description: TODO
      payload:
        type: integer
    disableDelay:
      description: TODO
      payload:
        type: integer
    vehicleName:
      description: TODO
      payload:
        type: string
  parameters:
    id:
      description: The index. Starts with 1.
      examples:
        - "1"
        - "2"
        - "5"
  tags:
    general:
      name: General
    loadpoints:
      name: Loadpoints
    vehicles:
      name: Vehicles
    site:
      name: Site
